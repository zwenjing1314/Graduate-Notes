你不需要读几十篇论文。为了完成这篇毕业论文，建议分成“必须精读、需要理解、按需补充”三层。

## 一、必须精读的论文

这些论文直接支撑你的理论、方法和实验。

### 1. ColPali

[ColPali: Efficient Document Retrieval with Vision Language Models](https://arxiv.org/abs/2407.01449)

作用：

```
视觉文档检索基础
多向量表示
ColBERT MaxSim
ViDoRe 评测
```

你已经完成复现，还需要补齐：

- 模型结构；
- 训练数据和损失；
- patch/query 表示；
- MaxSim；
- 与文本检索的区别；
- 官方实验结果。

### 2. ColBERT

[ColBERT: Efficient and Effective Passage Search via Contextualized Late Interaction](https://arxiv.org/abs/2004.12832)

作用：

```
解释 ColPali 为什么使用多向量和 Late Interaction。
```

重点读：

- token-level embedding；
- MaxSim；
- query/document 编码；
- 对比训练；
- 多向量存储代价。

### 3. ViDoRe Benchmark V1/V2

- [ColPali 论文](https://arxiv.org/abs/2407.01449) 中的 ViDoRe V1
- [ViDoRe Benchmark V2](https://arxiv.org/abs/2505.17166)

作用：

```
解释你的评价指标、数据集和实验协议。
```

重点读：

- 数据集组成；
- QA 格式；
- nDCG@5、Recall@1、Recall@5；
- V1 与 V2 的差异；
- 页面级评价的局限。

### 4. Patch-to-Region / Snappy

[Spatially-Grounded Document Retrieval via Patch-to-Region Relevance Propagation](https://arxiv.org/abs/2512.02660)

作用：

```
你的区域定位 baseline 和最直接的相关工作。
```

重点读：

- patch-BBox 坐标映射；
- IoU/intersection 分数传播；
- 两阶段检索；
- 区域级指标；
- training-free 方法的局限。

建议实现一个简化版本，但不必完整复制整个 Snappy 系统。

### 5. RegionRAG

[RegionRAG: Region-level Retrieval-Augmented Generation for Visual Document Understanding](https://arxiv.org/abs/2510.27261)

作用：

```
说明区域级检索和区域级监督已经有研究基础。
```

重点读：

- global document-query alignment；
- local region-query alignment；
- 区域级训练；
- patch 分组；
- 双目标损失；
- 与 Snappy 的差异。

不要求完整复现。

### 6. ColParse

[Beyond the Grid: Layout-Informed Multi-Vector Retrieval with Parsed Visual Document Representations](https://arxiv.org/abs/2603.01666)

作用：

```
支撑布局感知局部/全局表示和效率分析。
```

重点读：

- 外部文档解析；
- 区域裁剪；
- 局部向量与全局向量；
- 融合公式；
- 向量数量和存储；
- 与你的“复用原始 patch”方案的差异。

### 7. MM-Matryoshka

[MM-Matryoshka](https://arxiv.org/abs/2606.07654)

作用：

```
支撑多向量检索中的效率、层数和维度压缩分析。
```

重点读：

- layer/dimension 两维预算；
- prefix representation；
- distillation；
- 存储、延迟和精度曲线。

它不是你的核心结构模块，作为效率相关工作阅读即可。

## 二、必须理解但不必完整精读的论文

### 8. VisRAG

[VisRAG](https://arxiv.org/abs/2410.10594)

作用：

```
视觉文档 RAG 的任务背景和应用动机。
```

重点是理解：

- 页面图像如何进入 RAG；
- 为什么 OCR-only 方法会损失视觉布局；
- 整页检索为什么会带来无关内容。

### 9. LayoutLMv3

[LayoutLMv3](https://arxiv.org/abs/2204.08387)

作用：

```
学习 OCR token、视觉 patch 和二维布局如何联合建模。
```

重点读：

- bbox embedding；
- patch embedding；
- Word-Patch Alignment；
- 文本、视觉和布局融合；
- 消融实验。

不要直接照搬它的文档理解任务。

### 10. UDOP

[UDOP](https://arxiv.org/abs/2212.02623)

作用：

```
理解统一的图像、文本、布局编码和多任务训练。
```

重点读：

- 统一多模态输入；
- 布局表示；
- Encoder-Decoder；
- 预训练任务；
- 生成式损失。

不要因为 UDOP 使用生成模型，就直接把生成损失加入你的检索模型。

### 11. DocLLM

[DocLLM](https://arxiv.org/abs/2401.00908)

作用：

```
理解语言模型如何结合文档布局信息。
```

重点读：

- 结构信息注入方式；
- 文本与坐标的交互；
- 它与 LayoutLMv3、UDOP 的区别。

它属于结构建模参考，不是你的直接 baseline。

## 三、补充基础论文

这些论文不用现在优先读，但写理论和设计损失时可能用到。

### 12. LoRA

[LoRA](https://arxiv.org/abs/2106.09685)

用途：

```
如果后续显存不足，用 LoRA 微调 ColPali。
```

只需理解：

- 低秩更新；
- 冻结主干；
- 可训练参数量；
- 显存优势。

### 13. DocLayNet

[DocLayNet](https://arxiv.org/abs/2206.01062)

用途：

```
了解布局类别、BBox 标注和文档区域数据。
```

注意：它是布局数据集，不是检索数据集。

### 14. ColPali 效率相关工作

建议按需阅读：

- Light-ColPali；
- DocPruner；
- MetaEmbed；
- CausalEmbed；
- MUVERA。

它们用于支撑：

```
多向量索引成本
patch 合并
向量剪枝
固定维度编码
```

不必全部复现。

## 四、建议的阅读顺序

### 第一阶段：建立基础

```
ColBERT
-> ColPali
-> ViDoRe V1/V2
```

目标：真正理解你的 baseline 和评价方法。

### 第二阶段：理解区域检索

```
Snappy
-> RegionRAG
-> ColParse
```

目标：明确已有方法分别解决“区域定位”“训练型区域检索”“布局多向量和效率”的哪一部分。

### 第三阶段：理解结构融合

```
LayoutLMv3
-> UDOP
-> DocLLM
```

目标：为 OCR、BBox、视觉 patch 融合寻找理论依据。

### 第四阶段：补充效率和训练

```
MM-Matryoshka
-> LoRA
-> 其他压缩方法
```

目标：决定是否加入训练和效率扩展。

## 五、论文与实验的对应关系

| 论文          | 主要支撑的论文部分         |
| ------------- | -------------------------- |
| ColBERT       | 多向量和 MaxSim 理论       |
| ColPali       | 视觉检索 baseline          |
| ViDoRe        | 数据集和评价协议           |
| Snappy        | 区域定位 baseline          |
| RegionRAG     | 区域级训练和监督参考       |
| ColParse      | 局部/全局表示与效率对照    |
| MM-Matryoshka | 多向量压缩和成本分析       |
| LayoutLMv3    | OCR、patch、BBox 融合      |
| UDOP          | 统一多模态建模和多任务训练 |
| DocLLM        | 文档结构信息注入           |
| LoRA          | 参数高效微调               |
| DocLayNet     | 布局标注和区域类型         |

## 六、你真正需要完成的最低阅读集合

如果时间紧，最低读完这 8 篇即可：

```
ColBERT
ColPali
ViDoRe V2
Snappy
RegionRAG
ColParse
LayoutLMv3
MM-Matryoshka
```

UDOP、DocLLM、LoRA、DocLayNet作为补充。

## 七、阅读完成后的论文理论框架

读完后，你的论文理论章节可以形成：

```
传统文本检索
-> ColBERT 多向量检索
-> ColPali 视觉页面检索
-> ViDoRe 评价体系
-> Snappy/RegionRAG 区域级检索
-> LayoutLMv3/UDOP 文本-视觉-布局融合
-> ColParse/MM-Matryoshka 效率优化
-> 本文方法
```

你的实验章节则对应：

```
BM25/BGE-M3/Hybrid
-> ColPali
-> Snappy-style baseline
-> 你的结构融合方法
-> 页面级、区域级、效率和消融实验
```

当前最重要的是不要继续无限增加论文。先按顺序完成：

```
ColParse 一页笔记
RegionRAG 一页笔记
MM-Matryoshka 一页笔记
```

然后开始 MMDocIR 的 ColPali 基线实验和错误案例分析。