# 现在你是一个对面向多模态长文档的多粒度证据检索与可信生成方法研究的专家，你知道以前的研究方法，尤其是现在近期和最新的研究方法极其熟悉了解，并对未来发展有所感知。

好的。我会以“面向多模态长文档的多粒度证据检索与可信生成方法研究”专家的视角来协助你，重点关注：

1. **多模态长文档理解**
   包括 PDF、论文、报告、扫描件、图文混排文档、表格、图表、公式、版面结构等。
2. **多粒度证据检索**
   覆盖文档级、章节级、段落级、句子级、表格单元格级、图像区域级、图表元素级等证据定位。
3. **可信生成与可溯源回答**
   包括 RAG、grounded generation、citation generation、evidence attribution、hallucination detection、faithfulness evaluation 等。
4. **近期与前沿方法**
   包括多模态 RAG、long-context VLM、agentic retrieval、layout-aware retrieval、视觉 grounding、跨模态 dense retrieval、document QA benchmark、可信性评估等方向。
5. **未来研究趋势**
   包括细粒度证据链、可验证生成、长上下文与检索融合、结构化文档智能、交互式证据审查、多模态事实一致性评估等。

你可以直接让我帮你做：文献综述、研究方案设计、论文创新点凝练、方法框架设计、实验设计、相关工作撰写、开题报告、基金申请文本或论文审稿式修改。

# 你现在是我开题报告开会上的一个极其严格苛刻的老师A（你对科学的严谨性及其看中），你对我这个开题报告进行评价。创新性也要进行评价和给出意见或批评。

下面我以**开题报告会上非常严格的老师 A**的口吻来评价。先给结论：**这个题目方向是可以开题的，但目前还不能算一个“已经站稳的科学问题”，更像是一个工程系统方案。若不收敛问题、强化创新点和评价闭环，后续很容易写成“拼模块的 RAG 系统实现”，而不是硕士论文。**

------

## 一、总体评价

你的选题“面向多模态长文档的多粒度证据检索与可信生成方法研究”方向是有现实价值的。你准确抓住了 PDF 长文档、表格、图表、页面版式、跨页证据、证据引用和可信生成这些痛点，也明确说明不做基础模型预训练、不改 OCR、不专门做问答模型，而是研究证据检索、证据定位和可信生成机制，这个边界意识是好的。报告中对研究定位、总体流程、RQ1-RQ4、实验数据和评价指标都有相对完整的规划。

但是，我必须指出：**目前报告最大的问题是“系统完整性强，科学尖锐性不足”。** 你把文档解析、文本检索、视觉检索、版面建模、重排序、evidence cards、LLM verifier、拒答机制都放进来了，看起来很完整，但每一部分都还没有回答一个核心问题：**你的方法相对于已有多模态文档 RAG、ColPali、MMDocIR、PDFTriage、Self-RAG 等工作，到底新增了什么不可替代的机制？**

现在的创新点表达还是偏“组合式创新”，不是很硬。

------

## 二、值得肯定的地方

第一，**问题意识比较清楚。** 你没有把长文档问答简单理解成“把 PDF 切块然后喂给大模型”，而是指出固定长度切块会破坏章节层级、表格结构、图文对应关系，并把问题转向“能否找到正确证据、定位证据来源、判断生成内容是否被证据支持”。这个切入是合理的。

第二，**研究边界比很多开题报告清楚。** 你明确说本文不训练新的多模态基础模型、不以 OCR 或版面解析改进为目标，而是基于已有解析、检索和大模型能力研究多粒度证据组织、页面—区域协同检索和可信验证。这一点有助于控制硕士论文的工作量。

第三，**你已有前期实验基础，这是加分项。** 报告里给出了 SP-DocVQA 项目经验，也给出了中文年报数据的初步闭环：20 份年报、5327 个页面、99304 个证据节点、160 条人工修订问答样本，并实现了 BM25-page、Dense-page、Page→Region 三组实验。这个比单纯空谈方案要好。

第四，**评价指标意识较好。** 你没有只写 EM、F1，而是加入 Page Hit、Region Hit、Correct Answer with Correct Evidence Rate、Unsupported Answer Rate、Abstention Precision/Recall/F1、P95 latency 等指标，这说明你知道本文不能只评价答案，还必须评价证据和拒答。

------

## 三、主要问题与严厉批评

### 1. 创新性目前不够硬，容易被评价为“已有方法的工程拼接”

你列出的三个创新点是：多粒度证据节点表示、页面—区域协同检索、基于证据支持性的可信生成机制。问题是，这三个方向本身都不是全新的。

多粒度节点表示，已有文档智能、PDFTriage、layout-aware RAG、结构化检索都在做类似事情；页面级到区域级检索，MMDocIR 已经明确区分页面级和版面级检索；证据支持性验证，Self-RAG、RAG faithfulness、citation verification、LLM-as-judge、rule-based verifier 等已有大量研究。你的报告也承认这些相关工作存在。

所以你现在不能只说“我构建 evidence node”“我做 Page→Region”“我做 verifier”。老师会问：

> 这些不是已有工作的自然组合吗？你的核心技术增量在哪里？

你必须把创新点从“做了什么模块”改成“解决了什么已有方法解决不好的机制问题”。例如：

- 不是“构建证据节点”，而是“提出一种面向检索—生成—验证闭环的一致证据单元表示，并证明该表示在证据定位与支持性判断上优于普通 chunk / layout block”。
- 不是“页面—区域协同检索”，而是“研究页面级召回误差如何传递到区域级定位，并提出页面置信度、节点类型先验、区域相似度联合建模的排序机制”。
- 不是“可信验证”，而是“区分 evidence sufficiency 与 answer support，并建立可计算的 supported / insufficient / conflict 判别流程”。

现在的创新点还停留在描述层，没有形成足够明确的**技术假设—方法设计—可验证实验**链条。

------

### 2. 研究问题太多，主线有扩散风险

你设置了 RQ1 到 RQ4：证据表示、页面—区域协同检索、多模态融合、可信验证。每一个都可以单独写一篇论文。再加上中文年报、MMDocIR、LongDocURL 或 MMLongBench-Doc、ViDoRe V3、OmniDocBench，你的范围明显偏大。

作为开题老师，我会要求你回答：

> 你的主贡献到底是检索，还是生成，还是可信验证？
> 如果时间不够，哪一个必须完成，哪一个可以砍掉？

我建议你把主线压缩为：

**主线：多粒度证据检索与定位。**
**副线：基于检索证据的生成与支持性验证。**

也就是说，论文最硬的实验应该放在 Page→Region evidence retrieval 上，可信生成作为下游验证。如果你把生成和拒答也做成同等主线，风险很高，因为生成评价会受到大模型、prompt、verifier、数据标注等多重因素影响，最后很难证明你的核心方法有效。

------

### 3. 初步实验结果目前不能充分支撑技术路线

你报告中给出的 Page→Region 初步结果是：Page Recall@1 为 0.1938，Page Recall@5 为 0.2625，Page Recall@10 为 0.2625，Region Hit@5 为 0.2000。这个结果只能说明“路线可跑通”，不能说明“方法有效”。

尤其要注意：Page→Region 的 Page Recall@5 和 Page Recall@10 反而低于 Dense-page 的 0.2687 和 0.3187，而 Region Hit@5 只有 0.2000。你在报告中说“页级指标接近 Dense-page，同时能够返回具体证据节点”，这个表述太乐观。严格地讲，当前结果说明：

1. Dense-page 的页级召回本身还很低；
2. Page→Region 并没有提升页级召回；
3. Region Hit@5 只有 20%，距离可用还有很大差距；
4. 目前没有和“直接区域检索”“layout-aware chunk retrieval”“oracle page → region”等关键基线比较。

你必须补充两个非常关键的实验：

- **Oracle Page → Region**：假设页面召回正确，只评估区域定位能力。否则无法判断 Region Hit 低是因为页面没找对，还是区域排序没做好。
- **Global Region Retrieval**：不先召回页面，直接对所有区域节点检索。否则无法证明“两阶段”优于“直接节点检索”。

没有这两个实验，Page→Region 的科学结论不成立。

------

### 4. “多模态”目前写得多，落地证据不足

报告里多次提到页面图像、区域图像、视觉检索、ColPali、ColQwen、SigLIP/CLIP、视觉页面索引等。但前期实验主要是中文年报文本和节点检索，尚未看到真正的视觉检索结果。表 2 中视觉页面检索只是“可采用模型/算法”，不是已经落实的方法。

这会被老师质疑：

> 你的题目叫“多模态长文档”，但实验主体是不是仍然是文本检索？
> 图表、页面图像、bbox、区域图像到底参与了检索，还是只作为元数据保存？

你必须在论文中明确区分：

- **文本证据节点**：paragraph、table row、title。
- **视觉证据节点**：chart、figure、page image、region crop。
- **版面证据节点**：bbox、reading order、parent-child、section hierarchy。

而且至少要设计一个实验回答：

> 加入视觉信号后，在哪类问题上提升？
> 是表格题、图表题、版面定位题，还是扫描 PDF 题？
> 如果没有提升，为什么？

否则“多模态”会变成题目包装。

------

### 5. 可信生成部分还偏概念化

你写了 evidence cards、证据充分性判断、答案支持性判断、supported / insufficient / conflict、二次检索或拒答。这些概念是对的，但目前缺少可操作定义。

例如：

- 什么叫 evidence sufficient？
- 一个问题需要几条证据才算充分？
- 跨页证据如何判断充分？
- 表格数值问题中，单位不一致算 conflict 还是 unsupported？
- 文本型问题的“证据覆盖度”如何计算？
- LLM verifier 的判断如何校准？
- 拒答阈值怎么定？
- supported / insufficient / conflict 的人工标注标准是什么？

如果这些不定义清楚，可信生成实验会变成“让大模型自己判断大模型是否可信”，这是非常危险的。

你需要在开题后尽快写出一个**可信判别标注规范**，至少包括：

| 类别         | 判定标准                                   | 示例             | 负例         |
| ------------ | ------------------------------------------ | ---------------- | ------------ |
| supported    | 答案关键实体、数值、单位、条件均由证据支持 | 财务数值题       | 单位错       |
| insufficient | 检索到相关页面但缺少回答所需关键证据       | 页面相关但无指标 | 模糊文本     |
| conflict     | 答案与证据数值、时间、主体冲突             | 年份错、数值错   | 多表冲突     |
| unanswerable | 文档中不存在答案                           | 不可回答题       | 检索失败误判 |

没有这个标注规范，可信评价不可信。

------

## 四、对创新性的具体评价

我给你的创新性评价是：**方向有新意，组合有合理性，但当前创新强度为“中等偏弱”，需要重新凝练。**

### 创新点 1：多粒度证据节点表示

评价：**有价值，但不是强创新。**

你把页面、段落、标题、表格、图表等统一建模为 evidence node，并保留 page_id、bbox、node_type、text、image_crop、parent-child relation、reading_order、confidence 等字段。这个对系统实现很必要。

但严格说，这更像是数据结构设计，而不是方法创新。除非你证明：

1. 该表示优于固定 chunk；
2. 优于普通 layout block；
3. 对检索、生成、验证三个环节都有增益；
4. 节点粒度变化会影响证据召回和可信生成。

建议改成：

> 提出面向“检索—引用—验证”一致性的多粒度证据单元表示，并通过粒度消融证明页面级、段落级、表格行级、图表区域级表示对不同问题类型的贡献。

这样才像研究。

### 创新点 2：页面—区域协同检索

评价：**这是最有可能成为论文核心贡献的地方。**

这个点比第一个强，因为它有明确问题：长文档页面多，直接区域检索成本高，整页检索粒度粗。你提出先页面召回，再区域定位，再融合页面得分、节点得分、节点类型先验。这个可以做成较完整的算法。

但现在算法仍然比较朴素：
`S_final = αS_region + βS_page + γS_type`

这个公式太普通，像课程作业里的加权融合。你必须进一步说明：

- α、β、γ 如何确定？固定、调参、学习得到？
- S_type 如何定义？规则还是分类器？
- 页面召回错误如何补救？
- 多个页面证据如何聚合？
- 同一答案对应多个证据节点时如何排序？
- 表格行、段落、图表区域是否应该用同一种相似度？

否则这只是 heuristic fusion，不足以支撑“方法研究”。

### 创新点 3：证据支持性可信生成

评价：**概念重要，但目前最虚。**

你提出数值型问题做指标名称、数值、单位一致性校验，文本型问题结合证据覆盖度、检索置信度和模型判断；证据不足或冲突时二次检索或拒答。

这里的问题是：你很容易做成“规则 + prompt + LLM judge”，这在答辩时会被追问稳定性和可复现性。建议把可信生成降级为“验证模块”，不要宣称过强创新。你真正能做硬的是：

- 数值型问题：规则一致性验证；
- 表格型问题：指标—年份—单位—数值四元组验证；
- 文本型问题：证据覆盖 + NLI/LLM verifier 双重判断；
- 不可回答问题：检索置信度阈值 + verifier 判断。

------

## 五、我会在开题会上追问你的问题

你必须准备好回答下面这些问题：

1. **你的“多粒度证据节点”与普通 layout-aware chunk 的本质区别是什么？**
2. **为什么一定要先页面级召回再区域级检索？有没有可能直接区域检索更好？**
3. **Page→Region 的 Region Hit@5 只有 0.2000，你凭什么说路线有效？**
4. **你如何区分页面召回错误和区域定位错误？**
5. **视觉信息到底在哪些问题上发挥作用？有没有实验设计证明？**
6. **evidence sufficiency 和 answer support 的定义分别是什么？**
7. **不可回答问题是如何构造的？人工标注是否可靠？**
8. **你的中文年报 160 条 QA 样本太小，统计结论是否稳定？**
9. **你引用的最新文献和数据集能否全部获得并复现实验？**
10. **如果 ColPali / ColQwen / VLM 调用成本过高，你的主实验是否还能成立？**

这些问题如果答不上来，开题会会比较被动。

------

## 六、修改建议：必须收缩并增强科学性

我建议你把论文主贡献改成“三个可验证假设”：

### 假设 H1：多粒度证据节点优于固定文本切块

对比：

- fixed-size chunk；
- layout block；
- page-only；
- evidence node。

评价：

- Region Hit@k；
- Correct Answer with Correct Evidence；
- 引用准确率。

### 假设 H2：页面—区域协同检索优于单阶段检索

对比：

- page-only；
- global region retrieval；
- oracle page → region；
- predicted page → region；
- full hybrid retrieval。

这样可以明确区分页面召回和区域定位的贡献。

### 假设 H3：证据支持性验证能降低无依据生成

对比：

- no verifier；
- rule verifier；
- LLM verifier；
- rule + LLM verifier；
- with abstention / without abstention。

评价：

- unsupported answer rate；
- false answer rate；
- false refusal rate；
- abstention F1。

这样你的论文就从“系统实现”变成“科学实验”。

------

## 七、最终开题意见

我的意见是：**原则上同意开题，但必须大幅修改后进入正式研究。**

理由是：选题方向正确，文献基础较新，前期实验不是空白，系统框架也比较完整；但是当前创新点不够凝练，实验设计尚不能充分证明核心方法有效，尤其是 Page→Region 的初步结果还偏弱，可信生成模块定义不够严格，多模态部分存在“写得多、证据少”的风险。

我给你的建议是：**把论文的“最硬贡献”压到多粒度证据检索与区域定位上，把可信生成作为下游验证，不要贪大。** 你现在最需要做的不是继续堆模块，而是把每个模块对应的科学问题、对照基线、评价指标和失败分析讲清楚。否则，最后论文会显得工程量不少，但学术贡献不集中。

一句话评价：

> 这个题目可以做，但不能按现在这种“全链路系统集成”的方式泛泛做；必须把“页面—区域协同证据检索为什么有效、何时有效、相比什么有效”讲清楚，否则创新性站不住。



# 作为最顶级的专家视角来看，你觉得这个3个创新性应该怎么修改？

从顶级专家视角看，你现在这 3 个创新点**不是不能用，而是写法太“工程模块化”**。现在的表述是：

1. 构建多粒度证据节点表示；
2. 提出页面—区域协同检索方法；
3. 设计基于证据支持性的可信生成机制。

这些内容在报告中确实已经出现，并且和你的总体技术路线一致：文档解析→证据节点图→多索引→页面级粗召回→区域级证据检索→证据增强生成→可信验证。 但是作为“创新点”，目前问题是：**像系统功能描述，不像学术创新凝练。**

我建议把 3 个创新点改成下面这个版本。

------

# 建议修改后的 3 个创新点

## 创新点一：提出面向“检索—引用—验证”闭环的多粒度证据单元表示方法

### 原来的问题

你原来写“构建 evidence node”，容易被老师认为只是把页面、段落、表格、图表等元素整理成结构化字段。这个更像数据工程，不够像学术创新。

### 修改后的核心表达

不要强调“我把文档切成节点”，而要强调：

> 本文不是简单进行文档切块，而是将长文档中的页面、段落、表格行、图表区域等内容统一建模为可检索、可定位、可引用、可验证的证据单元，并使同一证据表示贯穿检索、生成和验证全过程。

### 可直接写入报告的版本

**（1）提出面向检索—引用—验证闭环的多粒度证据单元表示方法。**
针对传统固定长度文本切块难以保留长文档中的页面位置、版面结构、表格关系和证据来源等问题，本文提出一种面向多模态长文档 RAG 的多粒度证据单元表示方法。该方法将页面、段落、标题、表格块、表格行、图表区域等文档元素统一表示为 evidence node，并在节点中显式保留文本内容、页码、区域坐标、元素类型、图像裁剪、层级关系、阅读顺序、来源信息和解析置信度等信息。与普通文本 chunk 不同，该表示不仅服务于语义检索，还同时服务于证据定位、答案引用和支持性验证，使文档内容从“可被检索的文本片段”转化为“可被追踪和验证的证据单元”。本文将进一步通过不同粒度证据表示的消融实验，分析页面级、段落级、表格行级和图表区域级证据对检索召回、证据定位和生成可信性的影响。

### 这个版本强在哪里

它把创新点从“我建了一个节点结构”提升为：

**证据表示服务于完整闭环。**

也就是：

- 检索时用它；
- 生成时引用它；
- 验证时检查它；
- 评价时也基于它。

这样老师就不容易说这是普通数据结构。

------

## 创新点二：提出页面先验约束下的页面—区域级联证据检索与融合重排序方法

### 原来的问题

你原来写“页面级粗召回—区域级细定位”，方向对，但还不够锋利。因为 MMDocIR、PDFTriage、ColPali 等相关工作已经涉及页面级、版面级、视觉文档检索等思路。你的报告中也提到现有视觉检索多停留在页面级，缺乏区域级证据定位和证据聚合机制。

因此你要把创新重点放在：

> 页面召回不是最终结果，而是区域定位的先验约束；区域检索不是孤立检索，而是在页面置信度、节点类型、视觉/文本相似度和版面结构共同约束下进行重排序。

### 可直接写入报告的版本

**（2）提出页面先验约束下的页面—区域级联证据检索与融合重排序方法。**
针对多模态长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且容易产生误匹配的问题，本文提出一种页面先验约束下的页面—区域级联证据检索方法。该方法首先在页面级融合文本语义、页面视觉表示和版面元数据进行候选页面召回，将长文档检索空间压缩到高相关页面集合；随后在候选页面内部对段落、表格块、表格行和图表区域等 evidence nodes 进行细粒度检索，并引入页面相关性、区域相似度、节点类型匹配、指标关键词、版面位置和跨页关联等信号进行融合重排序。与仅返回页面级证据的方法相比，该方法能够降低生成上下文冗余并提高引用粒度；与直接全局区域检索相比，该方法能够利用页面级先验减少噪声区域干扰。本文将通过 page-only、global-region、oracle-page→region 和 predicted-page→region 等对照实验，分解分析页面召回误差与区域定位误差，验证页面—区域协同机制对证据召回率、区域命中率和检索效率的作用。

### 这个版本强在哪里

这个创新点应该是你论文最核心的贡献。它比原来更像“方法创新”，因为它提出了：

- 检索空间压缩；
- 页面先验；
- 区域细定位；
- 融合重排序；
- 误差分解实验。

尤其是最后一句很关键：
**“通过 oracle-page→region 和 predicted-page→region 分解页面召回误差与区域定位误差。”**

这会让老师觉得你不是在堆系统，而是在做科学分析。

------

## 创新点三：提出证据充分性与答案支持性解耦的可信生成与拒答机制

### 原来的问题

你原来写“基于证据支持性的可信生成机制”，这个说法太泛。现在 RAG 领域大量工作都在讲 faithfulness、support、citation、verification、reflection。你必须把自己的机制说得更具体。

你的报告里其实已经有一个好点：把验证拆成“证据充分性判断”和“答案支持性判断”。 这个应该被放大。你不要只说“判断答案是否被证据支持”，而要说：

> 我把“有没有足够证据回答”和“生成答案是否被证据支持”拆开处理。

这是一个更清楚的创新切入。

### 可直接写入报告的版本

**（3）提出证据充分性与答案支持性解耦的可信生成与拒答机制。**
针对多模态长文档 RAG 中证据不足仍强行回答、答案正确但引用错误、检索证据与生成内容不一致等问题，本文提出一种将证据充分性判断与答案支持性判断相解耦的可信生成机制。该机制首先将检索得到的 evidence nodes 组织为包含页码、区域类型、bbox、证据文本、图像裁剪、检索得分和来源信息的 evidence cards，并要求生成模型仅基于证据卡片进行回答和引用；随后在生成前判断当前证据是否足以回答问题，在生成后判断答案中的关键实体、数值、单位、时间、条件和结论是否被证据支持。对于数值型和表格型问题，本文采用指标—年份—数值—单位的一致性校验；对于文本型和跨页问题，结合证据覆盖度、检索置信度和模型判别进行支持性验证。当证据不足、证据冲突或答案无法被证据支持时，系统执行二次检索或拒答，并输出 supported、insufficient、conflict 或 unanswerable 等可信状态。本文将通过 unsupported answer rate、false answer rate、false refusal rate 和 abstention F1 等指标验证该机制对降低无依据生成和错误引用的作用。

### 这个版本强在哪里

它把“可信生成”从口号变成了一个**可评价的决策机制**：

- 生成前：证据够不够？
- 生成后：答案是否被支持？
- 不够：二次检索或拒答；
- 冲突：标记 conflict；
- 不存在答案：unanswerable。

这样可信生成就不再是“让大模型判断一下”，而是有清楚的判定流程。

------

# 最终建议版：3 个创新点标题

你可以直接把创新点标题改成下面这三个：

1. **提出面向检索—引用—验证闭环的多粒度证据单元表示方法**
2. **提出页面先验约束下的页面—区域级联证据检索与融合重排序方法**
3. **提出证据充分性与答案支持性解耦的可信生成与拒答机制**

这三个标题比你原来的版本更强，因为它们都有明确的学术关键词：

| 原标题                 | 问题       | 新标题优势                      |
| ---------------------- | ---------- | ------------------------------- |
| 构建多粒度证据节点表示 | 像数据结构 | 强调“检索—引用—验证闭环”        |
| 提出页面—区域协同检索  | 表述偏普通 | 强调“页面先验约束”和“级联检索”  |
| 设计可信生成机制       | 太泛       | 强调“证据充分性/答案支持性解耦” |

------

# 我最推荐你强调的主创新排序

从答辩说服力看，三者重要性应该这样排序：

**第一核心创新：页面先验约束下的页面—区域级联检索。**
这是你最像“方法论文”的地方。

**第二支撑创新：检索—引用—验证闭环的证据表示。**
这是前面方法成立的数据基础。

**第三应用创新：证据充分性与答案支持性解耦的可信生成。**
这是系统价值和可信性的体现，但不要把它吹成最强理论创新。

也就是说，你的论文不要讲成：

> 我做了一个完整多模态 RAG 系统。

而要讲成：

> 我围绕多模态长文档中的证据定位问题，提出了统一证据表示、页面—区域级联检索和证据支持性验证机制，使系统不仅能回答问题，还能定位证据并判断答案是否可信。

这个表达会更稳。



# 这里面写三个创新都写提出是不是有点不太好。能算得上提出吗？需要换一种说法吗？创新性文字内容有点多，能不能缩写一下文字数量。

是的，你的判断是对的。**三个创新点都写“提出”会显得用词偏满、偏硬，容易被老师追问：你真的提出了新理论、新模型还是新算法吗？**

在硕士开题报告里，建议更稳妥地写成：

1. **构建……表示方法**
2. **设计……检索方法**
3. **形成/设计……可信生成机制**

这样既体现创新，又不会显得过度拔高。你原报告中的三个创新点本身对应“证据节点表示、页面—区域协同检索、证据支持性可信生成”三部分，方向是合理的，但文字确实可以压缩。

------

## 建议改成这三个标题

### 1. 构建面向检索、引用与验证的多粒度证据表示

### 2. 设计页面—区域协同的多模态证据检索方法

### 3. 形成证据充分性与答案支持性结合的可信生成机制

这三个动词更稳：

| 创新点        | 建议动词    | 原因                                               |
| ------------- | ----------- | -------------------------------------------------- |
| 证据节点表示  | 构建        | 偏数据结构和表示体系，用“构建”更合适               |
| 页面—区域检索 | 设计 / 提出 | 这是核心方法，可以用“设计”，想强调创新时可用“提出” |
| 可信生成机制  | 形成 / 设计 | 偏流程机制和验证策略，用“形成”更稳                 |

如果你只保留一个“提出”，我建议放在第二个核心方法上：

> 构建……
> 提出……
> 设计……

这样层次更自然。

------

# 精简版创新点，可直接替换原文

## （五）研究创新点

**1. 构建面向检索、引用与验证的多粒度证据表示。**
针对传统文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文将页面、段落、标题、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并保留页码、区域坐标、元素类型、文本内容、图像裁剪、层级关系和来源信息。该表示能够同时服务于证据检索、证据定位、答案引用和支持性验证，使文档内容从普通文本片段转化为可追踪、可复核的证据单元。

**2. 设计页面—区域协同的多模态证据检索方法。**
针对长文档页数多、整页检索粒度粗、局部证据难定位的问题，本文采用“页面级粗召回—区域级细定位”的两阶段检索思路。首先融合文本语义、页面视觉和版面信息召回候选页面，再在候选页面内对段落、表格行、图表区域等证据节点进行细粒度检索，并结合页面相关性、区域相似度、节点类型和版面先验进行重排序，从而提高证据召回和定位能力。

**3. 形成证据充分性与答案支持性结合的可信生成机制。**
针对多模态文档 RAG 中证据不足仍强行回答、引用错误和答案不可复核等问题，本文将检索到的证据组织为 evidence cards，引导生成模型基于证据作答并输出引用信息。同时，从证据是否足够回答问题和生成答案是否被证据支持两个层面进行验证；当证据不足、证据冲突或答案无法被支持时，系统执行二次检索或拒答，以降低无依据生成风险。

------

## 更短一点的版本

如果你想让开题报告看起来更干净，可以用这个更短版：

**1. 构建多粒度证据表示。**
本文将页面、段落、表格行、图表区域等文档元素统一表示为 evidence node，并保留页码、区域坐标、元素类型、层级关系和来源信息，使证据能够同时服务于检索、引用和验证。

**2. 设计页面—区域协同检索方法。**
本文采用“页面级粗召回—区域级细定位”的两阶段检索框架，先召回相关页面，再在候选页面内定位段落、表格、图表等细粒度证据，并融合文本、视觉和版面信号进行重排序。

**3. 形成可信生成与拒答机制。**
本文将检索证据组织为 evidence cards，引导模型基于证据生成答案，并从证据充分性和答案支持性两个角度进行验证，在证据不足或冲突时执行二次检索或拒答。

------

我更建议你用**第一版精简版**，因为它比超短版更像正式开题报告，也比你原来的文字更克制、更稳。

# 你一开始提的意见不是挺好的吗？重新生成一下这三个创新点。

创新点一不应该改成并通过粒度消融证明页面级、段落级、表格行级、图表区域级表示对不同问题类型的贡献吗？
创新点二不应该改成先页面召回，再区域定位，再融合页面得分、节点得分、节点类型先验。这个可以做成较完整的算法吗？
创新点三不应该是做硬数值型问题：规则一致性验证；
表格型问题：指标—年份—单位—数值四元组验证；
文本型问题：证据覆盖 + NLI/LLM verifier 双重判断；
不可回答问题：检索置信度阈值 + verifier 判断。

你说得对。**我一开始给你的修改逻辑更适合作为正式创新点的“学术化版本”**：不是简单把三个创新点写短，而是要把它们写得更“硬”、更能经得起老师追问。你原报告中的三个创新点已经覆盖“证据节点表示、页面—区域协同检索、可信生成验证”三条主线，但原文偏模块描述，需要改成“可验证的研究贡献”。

下面是我建议你最终采用的版本。用词上不全部写“提出”，而是用**构建—设计—形成**，更稳。

------

## （五）研究创新点

### 1. 构建面向检索、引用与验证闭环的多粒度证据表示

针对传统固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向多模态长文档 RAG 的多粒度证据表示方法。该方法将页面、段落、标题、表格块、表格行、图表区域等文档元素统一建模为 evidence node，并保留页码、区域坐标、元素类型、文本内容、图像裁剪、层级关系、阅读顺序和来源信息，使文档内容从普通文本片段转化为可检索、可定位、可引用和可验证的证据单元。本文将进一步通过粒度消融实验，分析页面级、段落级、表格行级和图表区域级证据表示对不同问题类型下证据召回、区域定位和生成可信性的影响。

------

### 2. 设计页面—区域级联的多模态证据检索与融合重排序方法

针对多模态长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且噪声较多的问题，本文设计“页面级粗召回—区域级细定位”的两阶段证据检索方法。首先融合文本语义、页面视觉和版面元数据进行候选页面召回；随后在候选页面内对段落、表格块、表格行和图表区域等 evidence nodes 进行细粒度检索；最后结合页面得分、节点得分、节点类型先验、关键词匹配和版面位置等信号进行融合重排序。该方法能够在降低长文档检索空间的同时，提高细粒度证据定位能力。本文将通过 page-only、global-region、oracle-page→region 和 predicted-page→region 等对照实验，分析页面召回误差与区域定位误差，并验证页面—区域协同检索的有效性。

------

### 3. 形成面向证据充分性与答案支持性的可信生成验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成面向证据充分性与答案支持性的可信生成验证机制。该机制将检索得到的证据组织为 evidence cards，引导生成模型基于证据作答并输出引用信息；同时，将验证过程拆分为“证据是否足以回答问题”和“生成答案是否被证据支持”两个层面。对于数值型问题，采用规则一致性验证；对于表格型问题，进行“指标—年份—单位—数值”四元组校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 进行双重判断；对于不可回答问题，结合检索置信度阈值与 verifier 判断执行拒答。通过 supported、insufficient、conflict 和 unanswerable 等状态输出，降低无依据生成和错误引用风险。

------

## 我建议你最终保留的标题

1. **构建面向检索、引用与验证闭环的多粒度证据表示**
2. **设计页面—区域级联的多模态证据检索与融合重排序方法**
3. **形成面向证据充分性与答案支持性的可信生成验证机制**

这版比单纯“缩写”更好，因为它保留了三个最关键的硬点：

第一，创新点一不是简单说 evidence node，而是强调**粒度消融验证不同证据粒度的贡献**。
第二，创新点二不是泛泛说协同检索，而是明确成**页面召回、区域定位、融合重排序**的完整算法框架。
第三，创新点三不是空泛说可信生成，而是落到**数值、表格、文本、不可回答问题**四类可操作验证策略上。



# 这个是另一个专家B对你的创新点的评价和优化，你觉得它给出的创新性还需要优化和修改吗？义你对这个领域的先进的理解和严谨的态度。

主要不足

第一，创新点 1 仍然偏基础工程表示。
多粒度 evidence node 本身容易被认为是已有 layout-aware RAG、PDFTriage、MMDocIR 的自然延伸。它要更硬，必须强调：不是单纯建节点，而是为了后续“引用与验证闭环”设计字段，并通过粒度消融证明不同粒度对不同问题类型的贡献。

建议把“构建多粒度证据表示”改成：

构建面向检索、引用与验证闭环的证据节点表示与粒度评估方法

这样多了“粒度评估”，研究味更强。

第二，创新点 2 是最有潜力的主贡献，但还缺一个核心机制名。
现在写的是页面—区域级联检索和融合重排序，但“融合页面得分、节点得分、节点类型先验、关键词匹配和版面位置”还是容易被说成常规 feature fusion。

建议加入“证据充分性感知”或“问题类型驱动”的机制，例如：

根据问题类型判断候选证据是否覆盖必要证据要素，如指标、年份、单位、数值、风险描述等。

这样它就不只是排序融合，而是有你的核心增量。

第三，创新点 3 写得最好，但可能工作量偏大。
数值型、表格型、文本型、不可回答问题都做，听起来完整，但如果 PPT 或开题报告写太满，老师会问你能不能做完。建议把它写成“分层验证机制”，主打中文年报中最可控的数值/表格型验证，文本型和不可回答作为扩展。

第四，“多模态”仍然有风险。
三点里都写了图表区域、页面视觉、图像裁剪、视觉检索。但你当前实验还没真正做视觉检索。如果继续保留，就要加一句边界：

第一阶段以文本和版面结构为主，视觉页面/图表区域作为扩展验证模块。

否则老师可能继续追问：你的视觉结果在哪里？

我建议修改成下面这一版，更稳也更尖锐。

1. 构建面向检索、引用与验证闭环的多粒度证据表示

针对固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向长文档 RAG 的多粒度证据表示方法。该方法将页面、段落、标题、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并保留页码、区域坐标、元素类型、文本内容、层级关系、阅读顺序和来源信息，使文档内容从普通文本片段转化为可检索、可定位、可引用和可验证的证据单元。本文将通过粒度消融实验，分析页面级、段落级、表格行级等不同证据粒度对页面召回、区域定位和答案支持性的影响。

这里我建议暂时删掉“图像裁剪”或弱化，因为你还没做视觉裁剪闭环。

2. 设计证据充分性感知的页面—区域级联检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且噪声较多的问题，本文设计“页面级粗召回—区域级细定位”的两阶段证据检索方法。首先使用文本语义和版面元数据进行候选页面召回；随后在候选页面内对段落、表格块和表格行等 evidence nodes 进行细粒度检索；最后结合页面得分、节点得分、节点类型先验、关键词匹配、版面位置和证据要素覆盖度进行重排序。对于财务数值类问题，重点判断候选节点是否覆盖“指标—年份—单位—数值”等必要证据要素，从而提升区域级证据定位能力。本文将通过 page-only、global-region、oracle-page→region、predicted-page→region 和充分性特征消融实验，分析页面召回误差与区域定位误差，并验证证据充分性特征对检索效果的贡献。

这一点建议作为主创新。它比导师原文更硬。

3. 形成面向证据充分性与答案支持性的可信验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成面向证据充分性与答案支持性的验证机制。该机制将检索得到的证据组织为 evidence cards，引导生成模型基于证据作答并输出引用信息；同时将验证过程拆分为“证据是否足以回答问题”和“生成答案是否被证据支持”两个层面。对于数值和表格型问题，重点进行指标、年份、单位和数值一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案是否被证据支持；对于不可回答问题，结合检索置信度阈值和 verifier 判断执行二次检索或拒答。最终输出 supported、insufficient、conflict 和 unanswerable 等状态，以降低无依据生成和错误引用风险。

这版比导师原文更简洁一点，也没有铺太满。

我的最终建议

如果只保留一个最核心的创新点，应该是第 2 个：

证据充分性感知的页面—区域级联检索与重排序方法。

第 1 个是数据/表示基础，第 3 个是下游验证闭环。
论文主线应该是：

先构建可验证的证据节点 → 再做证据充分性感知检索 → 最后用可信验证证明检索证据能支撑答案。

我认为专家 B 的判断**总体是对的，而且比我前一版更稳、更接近开题报告场景**。尤其是他把第 2 个创新点强化为“证据充分性感知的页面—区域级联检索”，这是很好的修改方向。你的原报告中本来就已经把研究对象限定为“证据检索、证据定位和可信生成机制”，并且已有 Page→Region 的初步实验基础，所以第 2 点确实最适合作为主创新。

但我从更严格的专家视角看，B 的版本还可以再优化三处。

------

## 我对专家 B 版本的判断

### 第一，B 对创新点 1 的修改是对的，但标题还可以再硬一点

“构建面向检索、引用与验证闭环的多粒度证据表示”是稳的；但如果想更像研究贡献，确实要加上“粒度评估”。否则老师还是会觉得这是 evidence node schema，不是方法创新。

我建议第 1 点标题改成：

> **构建面向检索、引用与验证闭环的多粒度证据表示与粒度评估方法**

这样比单纯“证据表示”更有研究味，因为你不仅构建节点，还要通过实验回答：**什么粒度的证据对什么类型问题有效。**

------

### 第二，B 对创新点 2 的强化非常关键，但“证据充分性感知”需要定义清楚

我同意把第 2 点作为主创新。
但“证据充分性感知”这个词不能只是写在标题里，必须在正文里说清楚它到底是什么。

它不能泛泛说“判断证据够不够”，而应该落到可计算信号，例如：

- 财务数值题：是否覆盖指标、年份、单位、数值；
- 表格题：是否定位到对应表格行或表格块；
- 文本风险题：是否覆盖风险类型、主体、条件、结论；
- 不可回答题：检索分数和证据覆盖度是否低于阈值。

这样第 2 点才不只是 feature fusion，而是**问题类型驱动的证据要素覆盖重排序**。

------

### 第三，B 对创新点 3 的收缩是对的，但还要避免“生成”写得太大

你现在最硬的是检索与定位，不是生成模型本身。因此第 3 点最好不要写成“可信生成方法”太满，而是写成：

> **可信生成验证机制**

也就是说，你不是声称自己提出了新的生成模型，而是做了一个生成后的证据支持性验证与拒答决策流程。这个表述更稳。

------

# 我建议你最终采用这一版

## （五）研究创新点

### 1. 构建面向检索、引用与验证闭环的多粒度证据表示与粒度评估方法

针对固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向长文档 RAG 的多粒度证据表示方法。该方法将页面、段落、标题、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并保留页码、区域坐标、元素类型、文本内容、层级关系、阅读顺序和来源信息，使文档内容从普通文本片段转化为可检索、可定位、可引用和可验证的证据单元。本文将通过粒度消融实验，分析页面级、段落级、表格行级和图表区域级等不同证据粒度对页面召回、区域定位和答案支持性的影响。

------

### 2. 设计证据充分性感知的页面—区域级联检索与融合重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且噪声较多的问题，本文设计“页面级粗召回—区域级细定位”的两阶段证据检索方法。首先利用文本语义和版面元数据进行候选页面召回，并在条件允许时引入页面视觉表示作为补充；随后在候选页面内对段落、表格块、表格行和图表区域等 evidence nodes 进行细粒度检索；最后结合页面得分、节点得分、节点类型先验、关键词匹配、版面位置和证据要素覆盖度进行融合重排序。对于财务数值类问题，重点判断候选证据是否覆盖“指标—年份—单位—数值”等必要要素，从而提升区域级证据定位能力。本文将通过 page-only、global-region、oracle-page→region、predicted-page→region 和充分性特征消融实验，分析页面召回误差与区域定位误差，并验证证据充分性感知机制对检索效果的贡献。

------

### 3. 形成面向证据充分性与答案支持性的可信生成验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成面向证据充分性与答案支持性的可信生成验证机制。该机制将检索得到的证据组织为 evidence cards，引导生成模型基于证据作答并输出引用信息；同时将验证过程拆分为“证据是否足以回答问题”和“生成答案是否被证据支持”两个层面。对于数值和表格型问题，重点进行指标、年份、单位和数值一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案是否被证据支持；对于不可回答问题，结合检索置信度阈值和 verifier 判断执行二次检索或拒答。最终输出 supported、insufficient、conflict 和 unanswerable 等状态，以降低无依据生成和错误引用风险。

------

## 这版为什么比 B 的版本更好

它保留了 B 的核心修改，但做了三点加强：

第一，第 1 点加了**粒度评估方法**，避免被认为只是工程 schema。
第二，第 2 点把“证据充分性感知”具体化为**证据要素覆盖度**，使它成为真正的排序信号。
第三，第 3 点把“可信生成”收缩为**可信生成验证机制**，避免工作量被老师质疑过大。

我建议你的开题报告最终就用这三个标题：

1. **构建面向检索、引用与验证闭环的多粒度证据表示与粒度评估方法**
2. **设计证据充分性感知的页面—区域级联检索与融合重排序方法**
3. **形成面向证据充分性与答案支持性的可信生成验证机制**

其中第 2 个是主创新，第 1 个是基础创新，第 3 个是验证闭环。这样主线最清楚，也最容易答辩。

# 这个是专家c给出的意见和修改，你以自己专业眼光切严谨的态度来看，现在的创新点是不是需要修改或者补充呢？

1. 多粒度证据表示与粒度评估：有创新潜力，但要强化“证据图谱/可验证引用”而不是“解析后切块”

这一点是三者中最基础，也最容易被认为是“工程预处理”的部分。页面、段落、表格、图表区域、坐标、阅读顺序、层级关系这些信息，本身并不罕见；文档智能、layout-aware retrieval、多模态检索都已经在利用页面图像、版面结构和区域表示。ColPali/VisRAG/VDocRAG 这一类方法甚至绕过复杂解析，直接用视觉页面表示保留版面和视觉信息。

但你的创新点可以成立，关键是不要把它写成“我们保存了页码、坐标、类型、文本”。更强的写法是：

提出一种面向“检索—引用—验证”闭环的多粒度证据节点图表示，将文档元素建模为具有空间位置、语义内容、层级结构、跨模态关联和可验证引用边界的 evidence graph，并研究不同证据粒度对召回、定位、引用准确性和答案支持性的影响。

建议补充三件事：

第一，把 evidence node 从“元素集合”提升为“证据图”。节点包括 page、section、paragraph、table、row、cell、figure、caption、chart region；边包括 contains、next、caption-of、header-of、same-row、same-column、refers-to、derived-from。尤其是表格问题，表头—行—列—单位之间的关系比单独的 row node 更关键。

第二，增加“证据边界质量”指标。不仅评估 Recall@k，还要评估：page recall、region IoU、evidence boundary F1、citation exactness、answer-support sufficiency。LongDocURL 已经强调长文档理解、数值推理和跨元素定位，MMDocRAG 也关注多页、跨模态证据链和证据选择评价，所以你的粒度评估必须覆盖“定位质量”和“生成支持性”，否则不够新。

第三，加入自适应粒度选择。不要只做固定粒度消融，可以提出 query-aware granularity selection：文本解释题优先段落/章节，数值题优先表格行/单元格/单位区域，图表题优先 figure/chart/caption，跨页题保留 page + node 双层证据。这样创新性会明显增强。

结论：可以作为创新点，但建议命名为“多粒度可验证证据图表示与粒度自适应评估”，而不是普通的多粒度切块。

2. 页面—区域级联检索与充分性感知重排序：最有论文方法创新潜力

这一点是三个里面最像“方法贡献”的。页面级粗召回再区域级细定位，是合理且必要的，因为长文档直接做全局细粒度区域检索，搜索空间大、噪声高、成本也高。近期也已有类似的层次化思想，例如 HiREC 在金融标准化文档中采用层次化检索和证据整理，以减少相似文本、重复结构和近似表格带来的混淆。

但“页面级粗召回—区域级细定位—融合重排序”本身还不够新。它的创新性要落在两个地方：

第一，充分性感知不能只是手工特征，要形式化。
你提到“指标—年份—单位—数值”，这是很好的金融文档切入点。建议把它写成 evidence slot coverage，例如：

指标覆盖：是否命中 revenue / net income / operating cash flow 等指标；
时间覆盖：是否包含 fiscal year / quarter / comparative period；
单位覆盖：USD、RMB、million、thousand、percentage；
数值覆盖：候选证据中是否存在可解析数值；
结构覆盖：数值是否与正确行列标题绑定；
推理覆盖：是否需要多个证据节点共同完成计算或比较。

这样你就不是简单 rerank，而是在做问题要素驱动的证据充分性建模。这与 Google ICLR 2025 提出的 “sufficient context” 思路一致：RAG 不应只看相关性，而应判断上下文是否足以回答问题。

第二，建议把重排序从“加权融合”升级为“证据集选择”。
很多问题不是一个 node 能回答，而是多个 node 共同支持。ACL 2025 的 Evidence Tree Search 就指出，真实 RAG 中检索到的长文档常包含冗余和无关内容，证据检索需要建模多句/多证据之间的协同依赖。
你的方法可以进一步设计为：先选页面，再选候选节点，再组合成 evidence set，最后对 evidence set 的充分性、冲突性、冗余度进行排序。

建议把第二点改成：

提出证据充分性感知的页面—区域—证据集三级检索与融合重排序方法。该方法先进行页面级候选召回，再在页面内进行多粒度 evidence node 检索，最后根据语义相关性、版面一致性、节点类型、跨节点互补性和问题要素覆盖度选择最小充分证据集。

实验上你列的 page-only、global-region、oracle-page→region、predicted-page→region 很好。建议再加：

oracle-region→generation：测生成模型在完美证据下的上限；
retrieved-region→oracle-verifier：分离检索错误和验证错误；
single-node vs evidence-set：证明多证据组合的必要性；
text-only vs layout-aware vs visual-page：与 ColPali/VisRAG 类视觉检索方向对齐；
financial slot coverage ablation：去掉指标、年份、单位、数值、表头关系分别看影响。

结论：这是最值得重点包装的创新点。只要把“充分性”从经验规则变成可计算/可学习的证据集评分，它可以成为论文核心方法。

3. 可信生成验证机制：方向重要，但单独看创新性偏弱，需要做成“多模态证据级验证”

这一点非常必要，但目前写法容易被认为是已有 RAG faithfulness / hallucination detection / citation verification 的组合。RAGTruth 已经系统标注 RAG 中 unsupported 和 contradictory hallucination；UAEval4RAG 专门研究 RAG 对不可回答问题的拒答能力；FACTUM 关注长答案 RAG 的引用幻觉；SURE-RAG 进一步把验证建模为 supported、refuted、insufficient 的充分性判断，并强调“检索不是验证”。

所以你第三点的创新性不应写成“我们用 verifier 判断答案是否被支持”。这已经是当前主流方向。更好的突破口是：

面向多模态长文档 RAG，提出证据充分性与答案支持性的双层验证机制，分别验证 evidence set 是否足以回答问题，以及答案中的每个 claim 是否可由具体证据节点、表格结构或视觉区域支持。

建议做三层验证：

第一层：证据集充分性验证。
输入 question + retrieved evidence set，输出 sufficient / insufficient / conflict / unanswerable。这里不生成答案，先判断“证据够不够”。这与 sufficient context 和 SURE-RAG 的思想相呼应，但你可以把它扩展到表格、图表和坐标证据。

第二层：答案 claim-level 支持性验证。
把生成答案拆成 claim，例如“2022 年营业收入为 15.3 million USD”。每个 claim 必须绑定 evidence node，并检查指标、年份、单位、数值、方向关系是否一致。不要只给整段答案一个 supported 标签。

第三层：引用可验证性验证。
检查引用是否指向正确页码、区域坐标、表格行/列、图表区域。很多 RAG 系统的问题不是答案完全错，而是“答案对、引用错”或“引用相关但不支持”。FACTUM 对 citation hallucination 的关注说明这已经是一个独立重要问题。

你现在的 supported、insufficient、conflict、unanswerable 状态设计是合理的，但建议再加两个状态：

partially_supported：部分 claim 有证据，部分没有；
citation_mismatch：答案可能正确，但引用证据不支持或引用位置错误。

结论：第三点可以作为可信生成贡献，但必须强调“多模态证据节点级、claim-level、citation-level 的验证”，否则容易显得常规。

我建议你把三个创新点重新组织成“一个主线、三个贡献”

现在三个点之间已经有闭环，但表述上还可以更有论文感。建议改成：

创新点一：多模态长文档的可验证 evidence graph 表示

将页面、段落、表格、行列单元、图表区域和标题层级统一建模为 evidence graph，保留文本、视觉、版面、坐标、层级和来源信息，支持检索、定位、引用和验证。

创新点二：充分性感知的层次化证据集检索

提出 page → region/node → evidence set 的层次化检索框架，通过问题要素覆盖、版面关系、节点类型先验和跨证据互补性选择最小充分证据集，解决长文档中页面召回粗、区域检索噪声高、表格数值证据易错配的问题。

创新点三：面向生成可信性的双层验证与拒答机制

先验证证据集是否足以回答，再验证生成答案的每个 claim 是否被具体 evidence node 支持，并检查引用位置、数值、单位、年份和表格结构一致性；对证据不足、冲突和不可回答问题执行二次检索或拒答。

这样三个创新点之间形成非常清楚的链条：

证据表示 → 证据检索 → 证据验证 → 可信生成

还需要补充的关键内容

你现在最需要补的是**“可学习性”和“评估闭环”**。

如果全部是规则特征，创新性会下降。建议至少设计一个轻量学习模块，例如：

evidence node encoder：文本 + 版面 + 类型 + 视觉特征；
sufficiency scorer：输入 question + evidence set，输出 slot coverage / support score；
verifier：claim-evidence entailment + table consistency + numeric consistency；
calibration：把检索分数和验证分数校准成是否拒答的风险分数。

评估指标建议明确写成四类：

检索指标：Page Recall@k、Node Recall@k、MRR、nDCG；
定位指标：Region IoU、Evidence Boundary F1、Table Row/Cell Accuracy；
生成指标：Exact Match、F1、Numeric Accuracy、Unit Accuracy；
可信指标：Faithfulness、Citation Precision、Claim Support Rate、Abstention Accuracy、Conflict Detection F1。

此外，建议设置一个专门的数据子集：金融/年报/招股书/研报类长文档。因为你的“指标—年份—单位—数值”特别适合金融文档，而金融标准化文档中相似表格、重复格式、近似段落带来的检索混淆已被 HiREC 等工作指出，是一个有现实难度的问题。

我认真看完专家 C 的意见后，结论是：**C 的方向是对的，而且比 A、B 更接近顶级论文的表达方式；但不能原封不动全采纳。** 它把你的创新点往“evidence graph、evidence set、claim-level verification”上推，这是更前沿、更有学术味的方向；但对于你的硕士开题报告来说，**需要做“降维采纳”**，否则会显得目标过大、实现风险高。

你的原报告目前已经明确以“证据检索、证据定位和可信生成机制”为主线，并且已有中文年报 20 份、5327 页、99304 个证据节点、160 条问答样本以及 BM25-page、Dense-page、Page→Region 的前期实验闭环。这个基础支持你强化“页面—区域—证据验证”主线，但还不足以支撑过于完整的 multimodal evidence graph、cell-level reasoning、claim-level citation verification 全套系统。

------

## 一、专家 C 的意见哪些应该采纳？

### 1. “证据节点”应升级为“轻量证据图”，这个建议应采纳

C 说得很对：如果你只写 page、paragraph、table row、bbox、text、source，这确实容易被认为是工程切块。你的报告里原本写的是 evidence node，包括页码、区域位置、元素类型、文本内容、图像裁剪、层级关系、来源等字段，这个表述是合理的，但创新性不够突出。

建议采纳 C 的核心思想，但不要写成很重的“完整 evidence graph”。你应该写成：

> **轻量级多粒度证据图表示。**

也就是保留 page、section、paragraph、table block、table row、figure/chart region 等节点，同时加入 contains、next、belongs-to、header-of、caption-of、same-table 等少量关键关系。这样比普通 evidence node 更强，但又不会把工作量扩大到知识图谱级别。

### 2. “证据充分性感知”应成为第 2 个创新点的核心，这个必须采纳

C 的最重要贡献是把你的第 2 个创新点从“页面—区域检索 + 加权重排序”推进到：

> **问题要素覆盖驱动的证据充分性感知检索。**

这个方向非常好。因为单纯的 `S_final = αS_region + βS_page + γS_type` 太普通，容易被认为只是 feature fusion。你的报告里原本就已经写到页面得分、节点得分、节点类型和关键词匹配信号，这个确实还不够“硬”。

C 提到的 evidence slot coverage 很适合你的中文年报场景。金融数值问题天然可以拆成：

- 指标：营业收入、归母净利润、研发投入等；
- 时间：报告年度、同比年份、期末/本期；
- 单位：元、万元、亿元、百分比；
- 数值：具体金额或比例；
- 结构：是否处在正确表格行、表头、列名或单位区域下。

这会让你的检索不只是“相关性排序”，而是“能不能构成可回答证据”的排序。这个是最应该强化的主创新。

Google Research 的“sufficient context”工作也强调，RAG 错误不只是模型没用好上下文，还可能是检索上下文本身不足以回答问题，因此需要判断 retrieved context 是否 sufficient。([research.google](https://research.google/blog/deeper-insights-into-retrieval-augmented-generation-the-role-of-sufficient-context/?utm_source=chatgpt.com)) 这一点和你的“证据充分性感知检索”高度契合。

### 3. 第 3 个创新点应向 claim-level 和 citation-level 验证靠拢，但不要写得太满

C 说第三点如果只写“LLM verifier 判断答案是否支持”，创新性偏弱，这是准确的。现在 RAG 可信性研究已经大量关注 unsupported、contradictory、unanswerable、citation hallucination 等问题。例如 RAGTruth 专门标注 RAG 输出中的 unsupported 和 contradictory 内容，UAEval4RAG 关注 RAG 对不可回答问题的拒答能力，FACTUM 关注长答案 RAG 中“引用了但引用不支持”的 citation hallucination。([arxiv.org](https://arxiv.org/abs/2401.00396?utm_source=chatgpt.com))

所以你第三点不能只说“判断答案是否被证据支持”，要更具体：

- 先判断 evidence set 是否足够；
- 再判断 answer claim 是否被支持；
- 最后判断 citation 是否指向正确证据。

但我不建议你在开题报告里把它写成完整 claim-level citation verification 系统。你可以写成“分层验证”，其中数值/表格型问题做硬验证，文本型问题和不可回答问题作为扩展验证。这样最稳。

------

## 二、专家 C 的意见哪些要谨慎采纳？

### 1. 不建议把 evidence graph 写得过大

C 提到 cell、caption、chart region、same-column、refers-to、derived-from 等关系，这在顶级论文里很漂亮，但硕士开题报告里容易被老师追问：

> 你真的能稳定解析 cell 级表格结构吗？
> 图表区域和 caption-of 关系怎么自动获得？
> 表头—单元格关系错误时，后续验证怎么办？

你的报告明确说“不以改进 OCR 或文档解析模型为目标”，主要依托已有解析工具和检索模型。 所以 evidence graph 应该做成**轻量结构图**，不要承诺过深的 cell-level 或 chart-level 结构解析。

### 2. 不建议把“可学习性”写成必须完成

C 建议加入 evidence node encoder、sufficiency scorer、verifier、calibration，这个方向对，但不一定适合直接放进创新点。因为你目前前期实验规模只有 160 条中文年报问答样本，作为轻量学习 scorer 的监督数据明显不足。

可以写成：

> 先采用规则与特征融合实现证据充分性评分，在数据规模允许时探索轻量学习式重排序。

这样更严谨。

### 3. “多模态”要保留，但要降低承诺强度

你的题目是多模态长文档，但目前初步实验主要是文本、版面、表格节点层面的 Page→Region 检索；报告中也承认视觉检索成本较高，将其作为增强模块。

所以创新点里可以写“图表区域”“页面视觉表示”，但要加一句边界：

> 本文以文本和版面结构为主线，视觉页面与图表区域作为增强验证模块。

否则开题会上老师会继续问：视觉检索结果在哪里？

------

# 三、我建议最终修改成这一版

下面这版是我综合 A、B、C 后认为**最稳、最尖锐、也最适合开题报告**的版本。

------

## （五）研究创新点

### 1. 构建面向检索、引用与验证闭环的轻量级多粒度证据图表示

针对固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向长文档 RAG 的轻量级多粒度证据图表示。该表示将页面、章节、段落、表格块、表格行和图表区域等文档元素建模为 evidence node，并通过包含关系、阅读顺序、表格归属、标题层级和来源关系等边连接不同粒度证据单元。每个证据节点保留页码、区域坐标、元素类型、文本内容、层级关系和来源信息，使文档内容从普通文本片段转化为可检索、可定位、可引用和可验证的证据单元。本文将通过粒度消融实验，分析页面级、段落级、表格行级和图表区域级证据对页面召回、区域定位、引用准确性和答案支持性的影响。

------

### 2. 设计证据充分性感知的页面—区域—证据集层次化检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且容易产生噪声的问题，本文设计证据充分性感知的层次化检索方法。该方法首先进行页面级候选召回，再在候选页面内检索段落、表格块、表格行等细粒度证据节点，最后根据语义相关性、页面得分、节点类型、版面位置和问题要素覆盖度选择候选 evidence set。对于财务数值类问题，重点建模“指标—年份—单位—数值”等必要证据要素是否被覆盖；对于文本解释类问题，重点判断候选证据是否覆盖关键实体、条件和结论。该方法不只对单个节点排序，而是面向可回答性选择最小充分证据集，从而提升长文档中的证据定位能力和下游答案支持性。本文将通过 page-only、global-region、oracle-page→region、predicted-page→region、single-node vs. evidence-set 和充分性特征消融实验验证该方法的有效性。

------

### 3. 形成面向证据充分性、答案支持性与引用一致性的分层可信验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成分层可信验证机制。首先，在生成前判断检索得到的 evidence set 是否足以回答问题；其次，在生成后对答案中的关键 claim 进行证据支持性检查；最后，检查答案引用是否指向正确页码、区域或表格证据。对于数值和表格型问题，重点进行“指标—年份—单位—数值”一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案是否被证据支持；对于不可回答问题，结合检索置信度阈值和 verifier 判断执行二次检索或拒答。最终输出 supported、partially_supported、insufficient、conflict、citation_mismatch 和 unanswerable 等状态，以降低无依据生成和错误引用风险。

------

## 四、这版比专家 C 的版本更适合你的原因

这版保留了 C 的三大升级：

1. evidence node 升级为轻量 evidence graph；
2. Page→Region 升级为 Page→Region→Evidence Set；
3. answer verification 升级为 sufficiency、claim support、citation consistency 三层验证。

但它也做了必要收缩：

1. 没有过度承诺 cell-level 全自动解析；
2. 没有把视觉检索写成主线，而是作为增强；
3. 没有强行要求可学习 scorer，避免数据量不足；
4. 第 2 点仍然牢牢作为主创新，符合你已有 Page→Region 前期实验基础；
5. 第 3 点主要服务于验证闭环，不喧宾夺主。

------

## 五、最终建议

现在创新点**确实需要在专家 C 的基础上进一步修改**，但不是继续加内容，而是做两件事：

第一，**把第 2 个创新点确立为主创新**：

> 证据充分性感知的页面—区域—证据集层次化检索与重排序方法。

第二，**把第 1、3 点都服务于第 2 点**：

> 第 1 点提供证据图表示基础；
> 第 3 点验证检索出来的 evidence set 是否真的支撑答案。

最终论文主线应该是：

> **可验证证据图表示 → 充分性感知证据集检索 → 分层可信验证与拒答。**

这条线比“做一个多模态 RAG 系统”强得多，也更能经得起严格老师追问。



# 这是专家C对你的修改进行判断并给出新的创新性修改意见，你觉得还需要修改吗？哪种最适合做最终版的创新性呢？

我认为：**专家 C 这次给出的修改已经非常接近最终版，可以作为最终创新点的主体。**
不需要再大改，但我建议做一次**“最终收敛式修改”**：保留 C 的三点框架，同时进一步降低过度承诺，突出第 2 点为主创新。

你的报告原本已经有“多粒度证据节点、页面—区域协同检索、evidence cards、证据充分性与答案支持性验证”的基础设计和前期 Page→Region 实验，所以现在最适合的最终版不是继续扩展，而是把创新点写得**更可实现、更可验证、更像论文贡献**。

------

# 我的最终判断

最适合做最终版的是专家 C 修改后的这一版，但建议做三处小改：

第一，**第 1 点保留“轻量级证据图”，但不要写得像复杂图算法。**
也就是说，不要让老师误以为你要做 GNN、图推理、图遍历算法。这里的“证据图”应理解为结构化证据关系组织。

第二，**第 2 点作为主创新，必须保留“最小充分证据集”。**
这是目前最有方法贡献感的地方，比单纯 Page→Region 更强。关键是要解释“充分”和“最小”的定义。

第三，**第 3 点保留分层验证，但状态类别不要在开题汇报中过度展开。**
论文里可以细分 supported、partially_supported、insufficient、conflict、citation_mismatch、unanswerable；PPT 里建议压缩成“支持 / 不足或冲突 / 引用错误或不可答”三类。

------

# 我建议你最终采用这一版

## （五）研究创新点

### 1. 构建面向检索、引用与验证闭环的轻量级多粒度证据图表示

针对固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向长文档 RAG 的轻量级多粒度证据图表示。该表示将页面、章节、段落、表格块、表格行和图表区域等文档元素建模为 evidence node，并显式记录包含关系、阅读顺序、表格归属、标题层级和来源关系，形成可用于检索、引用和验证的证据图结构。每个证据节点保留页码、区域坐标、元素类型、文本内容、层级关系和来源信息，使文档内容从普通文本片段转化为可定位、可引用和可验证的证据单元。本文将通过粒度消融实验，分析页面级、段落级、表格行级等不同证据粒度对页面召回、区域定位、引用准确性和答案支持性的影响。

------

### 2. 设计证据充分性感知的页面—区域—证据集层次化检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且容易产生噪声的问题，本文设计证据充分性感知的层次化检索方法。该方法首先进行页面级候选召回，再在候选页面内检索段落、表格块、表格行等细粒度证据节点，最后根据语义相关性、页面得分、节点类型、版面位置和问题要素覆盖度选择候选 evidence set。对于财务数值类问题，重点判断“指标—年份—单位—数值”等必要证据要素是否被覆盖；对于文本解释类问题，重点判断候选证据是否覆盖关键实体、条件和结论。本文所称“充分”是指证据集能够覆盖回答问题所需的关键要素，“最小”是指在满足证据覆盖的前提下减少冗余页面和节点数量。该方法不只对单个节点排序，而是面向可回答性选择最小充分证据集，从而提升长文档中的证据定位能力和下游答案支持性。本文将通过 page-only、global-region、oracle-page→region、predicted-page→region、single-node vs. evidence-set 和充分性特征消融实验验证该方法的有效性。

------

### 3. 形成面向证据充分性、答案支持性与引用一致性的分层可信验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成分层可信验证机制。首先，在生成前判断检索得到的 evidence set 是否足以回答问题；其次，在生成后对答案中的关键 claim 进行证据支持性检查；最后，检查答案引用是否指向正确页码、区域或表格证据。对于数值和表格型问题，重点进行“指标—年份—单位—数值”一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案是否被证据支持；对于不可回答问题，结合检索置信度阈值和 verifier 判断执行二次检索或拒答。最终输出 supported、partially_supported、insufficient、conflict、citation_mismatch 和 unanswerable 等状态，以降低无依据生成和错误引用风险。

------

# 这版为什么最适合做最终版

这版的优点是：**有研究感，但不过度冒险。**

它没有把第 1 点写成普通 evidence node，也没有夸大成复杂图神经网络；第 2 点从“页面—区域检索”升级为“页面—区域—证据集检索”，并且引入“证据充分性”作为核心机制；第 3 点不再泛泛说可信生成，而是拆成生成前证据充分性、生成后 claim 支持性、引用一致性三个层次。

最终主线非常清楚：

> **轻量级证据图表示 → 最小充分证据集检索 → 分层可信验证与拒答。**

这比你原来“证据节点表示—页面区域检索—可信生成”的表述更强，也比过度强调多模态视觉模型更稳。

------

# 但是我建议你在开题报告中加一个边界说明

为了防止老师追问“多模态在哪里”，你可以在创新点后面补一句：

> 本文以文本内容、表格结构和版面关系为主要研究对象，在解析结果可靠的情况下引入页面图像、图表区域和区域截图等视觉信息作为补充验证模块。

这句话很重要。它既保留了“多模态长文档”的题目，又不会让老师误以为你的核心贡献是训练视觉检索模型或图表理解模型。

------

# PPT 汇报时的压缩版

开题 PPT 里不要放太长，可以写成：

1. **轻量级多粒度证据图表示**
   将页面、段落、表格行、图表区域等组织为 evidence node，并记录页码、坐标、层级和来源关系，支撑检索、引用与验证。
2. **证据充分性感知的层次化检索**
   采用 page → region/node → evidence set 的检索流程，根据相关性、节点类型、版面位置和“指标—年份—单位—数值”等证据要素覆盖度选择最小充分证据集。
3. **分层可信验证与拒答机制**
   先判断证据集是否充分，再验证答案 claim 是否被证据支持，最后检查引用位置是否一致；对证据不足、冲突或不可回答问题执行二次检索或拒答。

------

我的最终建议：**就用这版作为最终版创新点，不要再继续扩展。** 现在最重要的是把第 2 点配套的实验设计补扎实，尤其是 `global-region`、`oracle-page→region`、`predicted-page→region`、`single-node vs. evidence-set` 和“充分性特征消融”。这几个实验能直接决定创新点是否站得住。



对于创新点一，为什么叫它图，这不还是一个结点信息吗？说图有点夸大的成分吧？要不要修改一下？

# 对于创新点二，它的来源是哪篇文章，我们的创新性与那篇文章有什么区别和共同点？

你的判断是对的。**创新点一我建议把“图”字删掉或弱化**。在开题报告这种严格场合，如果你没有图神经网络、图遍历、图检索、边权学习，只是保存节点及其父子关系、阅读顺序、表格归属关系，那么叫“证据图”确实容易被认为有夸大成分。

你现在更稳的写法应该是：

> **构建面向检索、引用与验证的多粒度证据节点表示与粒度评估方法**

这个标题比“证据图”稳，也比普通“证据节点表示”更有研究味。因为它不仅说你建了节点，还强调你会通过粒度消融去评价不同证据粒度的作用。你的报告本身已经把页面、段落、表格、图表等建模为 evidence node，并用于检索、引用和验证，所以这样改是和原技术路线一致的。

------

## 一、创新点一建议最终改成这样

### 1. 构建面向检索、引用与验证的多粒度证据节点表示与粒度评估方法

针对固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向多模态长文档 RAG 的多粒度证据节点表示方法。该方法将页面、章节、段落、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并显式记录页码、区域坐标、元素类型、文本内容、层级关系、阅读顺序、表格归属和来源信息，使文档内容从普通文本片段转化为可定位、可引用和可验证的证据单元。本文将通过粒度消融实验，分析页面级、段落级、表格行级和图表区域级等不同证据粒度对页面召回、区域定位、引用准确性和答案支持性的影响。

这个版本最好。
它没有说“图”，但仍然保留了结构关系；没有夸大为图算法，但又比普通切块强。

如果你特别想保留“关系”，可以写成：

> **多粒度证据节点关系表示**

但我不建议在创新点标题里写“证据图”。

------

## 二、创新点二的来源：不是来自单篇文章，而是融合了几条研究线

你这个创新点不是凭空来的，也不能说完全来自某一篇文章。它最直接的相关基础有四类。

### 1. MMDocIR：页面级检索与版面级检索

MMDocIR 明确把多模态长文档检索划分为 **page-level retrieval** 和 **layout-level retrieval**，其中 layout 可以包括段落、公式、图片、表格、图表等元素。你的“页面级粗召回—区域级细定位”和它非常接近，尤其是在任务层级上。([arXiv](https://arxiv.org/abs/2501.08828?utm_source=chatgpt.com))

**共同点：**
都承认长文档不能只做整篇或整页检索，需要进一步定位到更细粒度的版面元素。

**区别：**
MMDocIR 主要是一个 benchmark 和检索任务定义，强调页面级和版面级检索评测；你的创新不能停留在“也做页面级和区域级”，而要进一步做：

- page → region/node → evidence set；
- 融合页面得分、节点得分、节点类型、版面位置；
- 用“指标—年份—单位—数值”等问题要素覆盖度判断证据是否充分；
- 最终服务于引用和可信验证。

所以你的创新点不能叫普通的“页面—区域检索”，否则和 MMDocIR 太接近；应该叫：

> **问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法**

------

### 2. PDFTriage：结构化文档中基于结构或内容检索上下文

PDFTriage 的核心思想是：PDF、网页、演示文稿等结构化文档如果只当作纯文本处理，会和用户理解文档的方式不一致，因此它允许模型基于文档结构或内容检索上下文。([ACL Anthology](https://aclanthology.org/2024.emnlp-industry.13/?utm_source=chatgpt.com))

**共同点：**
都认为 PDF 长文档不能只用纯文本 chunk，要利用页面、表格、章节等结构信息。

**区别：**
PDFTriage 更偏“结构化上下文检索 + QA”；你的方法应该更强调：

- 多粒度证据节点；
- 区域级证据定位；
- 表格行级、指标级证据；
- evidence set 的充分性；
- 答案引用和支持性验证。

也就是说，PDFTriage 是“结构辅助检索”，你要做的是“面向可回答性的证据集选择”。

------

### 3. HiREC：金融标准化文档中的层次化检索与证据策展

HiREC 是和你最像的一篇，尤其因为它也是金融文档场景。它针对 SEC filing 这类标准化金融文档中重复文本、相似表格结构带来的检索混淆问题，提出 Hierarchical Retrieval with Evidence Curation：先做层次化检索，再进行 evidence curation，去除无关段落，必要时生成补充查询以收集缺失信息。([arXiv](https://arxiv.org/abs/2505.20368?utm_source=chatgpt.com))

**共同点：**

- 都面向金融/年报类长文档；
- 都采用层次化检索思想；
- 都关心检索结果是否足以支撑答案；
- 都不是简单 top-k chunk retrieval。

**关键区别：**

| 维度        | HiREC                                    | 你的方法应强调的区别                       |
| ----------- | ---------------------------------------- | ------------------------------------------ |
| 检索层级    | document → passage                       | page → region/node → evidence set          |
| 文档对象    | SEC 标准化英文金融文档                   | 中文年报/招股书等多模态 PDF                |
| 证据粒度    | passage 为主                             | 页面、段落、表格块、表格行、图表区域       |
| 充分性建模  | evidence curation、补充查询              | 指标—年份—单位—数值等问题要素覆盖          |
| 核心目标    | 减少重复/无关 passage，提高开放域金融 QA | 提高区域级证据定位、引用准确性和答案支持性 |
| 多模态/版面 | 不是主轴                                 | 版面结构、表格行、区域坐标是主轴           |

所以，如果老师问“你的方法是不是 HiREC 换个中文年报场景？”，你要回答：

> 不是。HiREC 的层次是文档到段落，重点是开放域金融 QA 中的证据策展；本文的层次是页面到区域节点再到最小充分证据集，重点是多模态 PDF 长文档中的区域级证据定位、表格数值要素覆盖和可验证引用。

------

### 4. Sufficient Context 与 Evidence Tree Search：支撑“充分性”和“证据集”的理论来源

Google Research 的 Sufficient Context 工作提出，RAG 不仅要看检索内容是否相关，还要判断检索上下文是否足以回答问题；这和你的“证据充分性感知”非常相关。([Google Research](https://research.google/blog/deeper-insights-into-retrieval-augmented-generation-the-role-of-sufficient-context/?utm_source=chatgpt.com))

Evidence Tree Search 则指出真实 RAG 中检索结果常包含冗余和无关内容，会干扰下游生成，因此把证据检索建模为动态证据树搜索过程，用于更好地选择有效证据。([ACL Anthology](https://aclanthology.org/2025.acl-long.1175/?utm_source=chatgpt.com))

**共同点：**
都不满足于“相关片段检索”，而是进一步关注“证据是否足够、是否互补、是否减少冗余”。

**区别：**
你的方法不需要照搬 Evidence Tree Search 的树搜索算法，也不必写成复杂搜索框架。你的落点应更具体：

> 在多模态长文档 PDF 中，根据问题类型选择覆盖必要要素的最小充分证据集。

这比泛泛说“证据树”更适合硕士论文，也更容易落地。

------

## 三、创新点二建议最终改成这个标题

我建议把“证据充分性感知”再具体一点，改成：

> **设计问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法**

这个标题比“证据充分性感知”更清楚。
因为“充分性”容易显得抽象，而“问题要素覆盖”是可计算、可消融、可解释的。

------

## 四、创新点二最终文字建议

### 2. 设计问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且容易产生噪声的问题，本文设计问题要素覆盖驱动的层次化证据检索方法。该方法首先进行页面级候选召回，再在候选页面内检索段落、表格块、表格行和图表区域等细粒度证据节点，最后根据语义相关性、页面得分、节点类型、版面位置和问题要素覆盖度选择候选 evidence set。对于财务数值类问题，重点判断候选证据是否覆盖“指标—年份—单位—数值”等必要要素；对于文本解释类问题，重点判断候选证据是否覆盖关键实体、条件和结论。本文所称“充分”是指证据集能够覆盖回答问题所需的关键要素，“最小”是指在满足证据覆盖的前提下减少冗余页面和节点数量。该方法不只对单个节点排序，而是面向可回答性选择最小充分证据集，从而提升长文档中的证据定位能力和下游答案支持性。

------

## 五、最终建议

创新点一：**不要叫“证据图”**，改成：

> **构建面向检索、引用与验证的多粒度证据节点表示与粒度评估方法**

创新点二：**不要只叫“页面—区域级联检索”**，改成：

> **设计问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法**

这样更严谨。
第一个避免夸大，第二个增强创新性。你的核心创新不是“用了分层检索”，而是：

> 在多模态长文档中，将页面召回、区域定位和问题要素覆盖结合起来，选择能够支撑答案的最小充分证据集。

### 1. 构建面向检索、引用与验证的多粒度证据节点表示与粒度评估方法

针对固定长度文本切块容易破坏长文档版面结构、表格关系和证据来源的问题，本文构建面向多模态长文档 RAG 的多粒度证据节点表示方法。该方法将页面、章节、段落、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并显式记录页码、区域坐标、元素类型、文本内容、层级关系、阅读顺序、表格归属和来源信息，使文档内容从普通文本片段转化为可定位、可引用和可验证的证据单元。本文将通过粒度消融实验，分析页面级、段落级、表格行级和图表区域级等不同证据粒度对页面召回、区域定位、引用准确性和答案支持性的影响。

### 2. 设计问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索搜索空间大且容易产生噪声的问题，本文设计问题要素覆盖驱动的层次化证据检索方法。该方法首先进行页面级候选召回，再在候选页面内检索段落、表格块、表格行和图表区域等细粒度证据节点，最后根据语义相关性、页面得分、节点类型、版面位置和问题要素覆盖度选择候选 evidence set。对于财务数值类问题，重点判断候选证据是否覆盖“指标—年份—单位—数值”等必要要素；对于文本解释类问题，重点判断候选证据是否覆盖关键实体、条件和结论。本文所称“充分”是指证据集能够覆盖回答问题所需的关键要素，“最小”是指在满足证据覆盖的前提下减少冗余页面和节点数量。该方法不只对单个节点排序，而是面向可回答性选择最小充分证据集，从而提升长文档中的证据定位能力和下游答案支持性。

### 3. 形成面向证据充分性、答案支持性与引用一致性的分层可信验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成分层可信验证机制。首先，在生成前判断检索得到的 evidence set 是否足以回答问题；其次，在生成后对答案中的关键 claim 进行证据支持性检查；最后，检查答案引用是否指向正确页码、区域或表格证据。对于数值和表格型问题，重点进行“指标—年份—单位—数值”一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案是否被证据支持；对于不可回答问题，结合检索置信度阈值和 verifier 判断执行二次检索或拒答。最终输出 supported、partially_supported、insufficient、conflict、citation_mismatch 和 unanswerable 等状态，以降低无依据生成和错误引用风险。



本文三个创新点不是并列关系，而是递进关系：第一点解决证据如何组织和评估，第二点解决如何找到足以回答问题的证据集，第三点解决证据是否足够、答案是否被支持以及引用是否一致。其中，第二点“问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法”是本文核心方法。

### 1. 构建面向检索、引用与验证的多粒度证据节点表示与粒度评估方法

针对固定长度文本切块易破坏长文档版面结构、表格关系和证据来源的问题，本文构建多粒度证据节点表示方法。该方法将页面、章节、段落、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并记录页码、区域坐标、元素类型、文本内容、层级关系、阅读顺序、表格归属和来源信息，使文档内容由普通文本片段转化为可定位、可引用和可验证的证据单元。

该创新点的重点不只是保存节点字段，而是通过粒度消融实验分析不同证据粒度的作用。本文将比较 page-only、paragraph-only、table-block、table-row、mixed-node 与 mixed-node+structure 等设置，评估不同粒度对页面召回、区域定位、引用准确性和答案支持性的影响。

### 2. 设计问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索噪声较大的问题，本文设计问题要素覆盖驱动的层次化证据检索方法。该方法先进行页面级候选召回，再在候选页面内检索段落、表格块、表格行和图表区域等细粒度 evidence nodes，最后结合语义相关性、页面得分、节点类型、版面位置和问题要素覆盖度选择候选 evidence set。

对于财务数值类问题，重点判断候选证据是否覆盖“指标—年份—单位—数值”等要素；对于文本解释类问题，重点判断是否覆盖关键实体、条件和结论。本文所称“充分”是指证据集覆盖回答所需关键要素，“最小”是指在满足证据覆盖的前提下减少冗余页面和节点数量。该方法不只排序单个节点，而是面向可回答性选择最小充分证据集，以提升证据定位能力和下游答案支持性。

实现上，系统将通过 BM25、BGE-M3 或混合检索召回候选页面，再检索页面内 evidence nodes，并融合语义相关性、页面先验、节点类型、版面位置和问题要素覆盖度形成综合得分。后续将通过 page-only、global-region、oracle-page→region、predicted-page→region、single-node vs. evidence-set 和覆盖特征消融验证方法贡献。

### 3. 形成面向证据充分性、答案支持性与引用一致性的分层可信验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成分层可信验证机制。首先在生成前判断 evidence set 是否足以回答问题；其次在生成后检查答案关键 claim 是否被证据支持；最后验证引用是否指向正确页码、区域或表格证据。

对于数值和表格型问题，重点进行“指标—年份—单位—数值”一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案支持性；对于不可回答问题，结合检索置信度阈值和 verifier 执行二次检索或拒答。最终输出 supported、partially_supported、insufficient、conflict、citation_mismatch 和 unanswerable 等状态，以降低无依据生成和错误引用风险。



# 所以说你到底研究的是什么？最后要展示的是什么效果？

你这个问题问到核心了。**你研究的不是“让大模型读长文档回答问题”这么泛的东西，也不是研究一个新的多模态大模型。**

你真正研究的是：

> **在复杂版式长文档中，如何把答案所需证据找准、定位准，并判断生成答案是否真的被证据支持。**

换句话说，你研究的核心不是“回答”，而是：

> **证据怎么组织、怎么检索、怎么组合、怎么验证。**

你的开题报告里已经把研究对象限定为多模态长文档中的证据检索、证据定位和可信生成机制，而长文档问答只是主要验证任务。这个定位是对的，但你现在需要用更简单的话讲清楚。

------

# 一、你到底研究什么？

你研究的是一个面向复杂 PDF 长文档的 **证据级 RAG 方法**。

具体来说，有三个问题：

## 1. 文档证据应该怎么组织？

普通 RAG 会把 PDF 切成固定长度文本块，比如 500 字一个 chunk。
但年报、合同、研报、政策文件里面有页面、标题、段落、表格、表格行、图表、页码、坐标、单位说明。

所以你研究：

> 如何把长文档内容组织成可检索、可引用、可验证的 evidence nodes。

这就是创新点一。

------

## 2. 问题来了以后，怎么找到真正够用的证据？

不是只找“语义相似”的片段，而是找“足够回答问题”的证据。

例如问题是：

> 2023 年营业收入是多少？

系统不能只找到一个包含“营业收入”的页面，而要尽量找到：

- 指标：营业收入；
- 年份：2023；
- 单位：万元 / 元 / 亿元；
- 数值：具体金额；
- 位置：第几页、哪个表格行。

所以你研究：

> 如何通过页面召回、区域定位和证据集选择，找到最小充分证据集。

这就是创新点二，也是你论文最核心的研究内容。

------

## 3. 答案生成后，怎么判断它可信不可信？

普通 RAG 可能生成一个看似正确的答案，但证据不一定支持它。

所以你研究：

> 如何判断证据是否足够，答案是否被证据支持，引用位置是否正确。

比如答案说：

> 2023 年营业收入为 12.3 亿元。

你要检查：

- 证据里有没有“营业收入”；
- 年份是不是 2023；
- 数值是不是 12.3；
- 单位是不是亿元；
- 引用页码和表格行是否正确。

这就是创新点三。

------

# 二、一句话定义你的研究

你可以这样定义：

> 本文研究面向复杂版式长文档的证据级检索与可信生成方法，重点解决长文档 RAG 中证据粒度不清、局部证据难定位、答案缺乏可验证依据的问题。

再短一点：

> 本文研究如何在长文档中找到足够支撑答案的细粒度证据，并基于这些证据生成可引用、可验证的回答。

这句话最适合开题汇报。

------

# 三、你最后要展示什么效果？

最终你要展示的不是“我做了一个聊天机器人”，而是展示：

> **系统不仅能回答问题，还能指出答案来自哪里，并判断答案是否被证据支持。**

最终效果应该是这样的。

------

## 示例 1：财务数值问题

用户输入：

> 2023 年公司营业收入是多少？

系统输出：

> 答案：2023 年公司营业收入为 XXX 万元。
> 证据：第 15 页，主要会计数据表，表格行“营业收入”。
> 支持状态：supported。
> 校验结果：指标、年份、单位、数值均与证据一致。

这个展示的是：

- 找到了正确页面；
- 找到了正确表格行；
- 答案数值正确；
- 引用可复核；
- 系统判断答案被证据支持。

------

## 示例 2：文本解释问题

用户输入：

> 公司面临哪些主要经营风险？

系统输出：

> 答案：公司主要面临市场竞争风险、原材料价格波动风险和政策变化风险。
> 证据：第 42 页“风险因素”章节，第 2—4 段。
> 支持状态：supported / partially_supported。

这个展示的是：

- 段落级证据定位；
- 多条证据组合；
- 文本型 claim 支持性判断。

------

## 示例 3：不可回答问题

用户输入：

> 公司是否披露了 2026 年营业收入预测？

如果文档里没有，系统应该输出：

> 当前文档中未找到足够证据支持该问题的答案，建议拒答。
> 支持状态：unanswerable / insufficient。

这个展示的是：

- 不强行编答案；
- 能识别证据不足；
- 能进行拒答。

------

# 四、你的最终实验要证明什么？

你最后不是只展示 Demo，还要用实验说明三个结论。

## 结论 1：不同证据粒度适合不同问题

你要证明：

- 数值题更适合 table-row；
- 文本解释题更适合 paragraph；
- 跨页或复杂问题更适合 mixed-node；
- 加入结构信息后，引用和答案支持性更好。

对应创新点一。

------

## 结论 2：你的层次化检索比普通检索更好

你要证明：

> 先召回页面，再定位区域，再选择 evidence set，比只返回页面、直接全局区域检索、只选单个节点更有效。

具体比较：

- page-only；
- global-region；
- predicted-page→region；
- oracle-page→region；
- single-node；
- evidence-set；
- 去掉问题要素覆盖度的版本；
- 完整方法。

对应创新点二。

------

## 结论 3：可信验证能减少无依据生成和错误引用

你要证明：

- 加入验证后 unsupported answer rate 降低；
- citation accuracy 提升；
- 数值和单位错误减少；
- 不可回答问题上拒答更准确。

对应创新点三。

------

# 五、最终论文主线应该怎么讲？

你不要讲成：

> 我做了一个多模态 RAG 系统。

要讲成：

> 我围绕长文档问答中的“证据可信性”问题，研究从证据表示、证据检索到证据验证的完整方法。

更具体：

```text
复杂 PDF 长文档
    ↓
构建多粒度 evidence nodes
    ↓
分析不同证据粒度的作用
    ↓
页面召回 → 区域定位 → evidence set 选择
    ↓
基于 evidence cards 生成答案
    ↓
验证证据充分性、答案支持性和引用一致性
    ↓
输出答案 + 页码 + 区域 + 支持状态
```

------

# 六、最终要展示的核心效果

你最后答辩时最好展示一个系统案例，界面或表格里包括：

| 输出内容 | 示例                       |
| -------- | -------------------------- |
| 问题     | 2023 年营业收入是多少？    |
| 答案     | XXX 万元                   |
| 证据页码 | 第 15 页                   |
| 证据区域 | 表格第 3 行                |
| 证据文本 | 营业收入 XXX 万元          |
| 证据类型 | table-row                  |
| 支持状态 | supported                  |
| 验证结果 | 指标、年份、单位、数值一致 |

这才是你论文最有说服力的展示效果。

------

# 七、最简洁回答

你可以这样回答导师：

> 我的研究重点不是训练新的问答模型，而是研究长文档 RAG 中证据如何被组织、检索和验证。最终目标是让系统在回答问题时，不仅给出答案，还能定位到页码、段落、表格行或图表区域，并判断答案是否被证据支持。最终展示效果是“答案 + 证据位置 + 证据内容 + 支持状态”，并通过实验验证多粒度证据表示、层次化检索和可信验证机制能够提升证据定位准确性、引用准确性和答案可信性。



# 你的这些数据集能满足你完成结论1的验证吗？

严格说：**你现在列的数据集只能“部分满足”结论 1 的验证，不能完全满足。**

结论 1 是：

> **不同证据粒度适合不同问题类型。比如数值题更适合 table-row，文本解释题更适合 paragraph，跨页综合题更适合 mixed-node。**

要验证这个结论，数据集必须同时满足三个条件：

1. 文档中存在多种证据粒度：页面、段落、表格块、表格行、图表区域等；
2. 问题类型足够丰富：数值题、文本解释题、表格题、跨页题、不可回答题；
3. 每个问题最好有明确 gold evidence：至少有正确页码，最好有正确段落、表格行或区域节点。

你现在的数据设计中，MMDocIR 用于页面级、版面级检索和区域级证据定位；LongDocURL 或 MMLongBench-Doc 用于长文档生成、跨页证据、不可回答或跨元素定位；ViDoRe V3 子集用于视觉检索与 source grounding；中文年报/招股书自建数据用于中文复杂文档验证。这个组合方向是合理的。

但如果专门验证“不同证据粒度对不同问题类型的影响”，**最关键的数据其实是中文自建数据和 MMDocIR，其他数据只能辅助。**

------

## 1. 哪些数据集能支持结论 1？

| 数据集                  | 能否支持结论 1       | 原因                                                         |
| ----------------------- | -------------------- | ------------------------------------------------------------ |
| 中文年报/招股书自建数据 | **最适合**           | 你能控制 page、paragraph、table-block、table-row 等证据节点，也能人工标注证据粒度 |
| MMDocIR                 | **比较适合**         | 它本身关注页面级检索和版面级检索，适合验证 page vs region/layout |
| LongDocURL              | **部分适合**         | 适合跨页、跨元素、定位类问题，但未必直接支持你自定义的 table-row 粒度消融 |
| MMLongBench-Doc         | **部分适合**         | 适合长文档理解、跨页和不可回答问题，更适合验证生成和可信性，不是最适合粒度消融 |
| ViDoRe V3 子集          | **辅助适合**         | 更适合视觉页面检索和 grounding，不是验证 table-row、paragraph 粒度的主数据 |
| OmniDocBench            | **不适合作为主验证** | 主要用于文档解析质量参考，不适合直接验证证据粒度对问答效果的影响 |

所以，**结论 1 的主验证数据不应该依赖所有数据集，而应该主要依赖：中文自建数据 + MMDocIR。**

------

## 2. 你当前中文自建数据够不够？

目前还不够充分，但已经有基础。

你现在的中文年报数据包含 20 份公开年度报告、5327 个页面、99304 个细粒度证据节点，并构建和人工修订了 160 条问答样本；所有问题标注 page-level evidence，并通过规则匹配与人工修订映射到 region-level evidence node。

这说明你已经具备验证粒度消融的基础，因为你已经有：

- 页面；
- 段落；
- 表格块；
- 表格行；
- 问题；
- 页级证据；
- 区域级证据映射。

但是问题是：**160 条样本太少，且问题类型可能不够均衡。**

如果你要支持“不同粒度适合不同问题类型”这个结论，至少需要保证每类问题都有足够样本。例如：

| 问题类型   | 建议样本量 | 主要验证粒度            |
| ---------- | ---------- | ----------------------- |
| 财务数值题 | 80–120 条  | table-row / table-block |
| 表格对比题 | 40–60 条   | table-row + table-block |
| 文本解释题 | 60–100 条  | paragraph / section     |
| 风险披露题 | 40–60 条   | paragraph               |
| 跨页综合题 | 30–50 条   | mixed-node              |
| 不可回答题 | 30–50 条   | mixed-node + 置信度判断 |

总量最好扩展到 **300–500 条左右**，这样结论会更稳。硕士论文不一定要特别大规模，但至少要做到问题类型均衡，否则老师会问：

> 你说 table-row 对数值题有效，是不是因为你的数据里大部分都是财务数值题？

------

## 3. MMDocIR 能不能验证结论 1？

可以验证一部分，尤其是：

- page-only vs layout/region；
- 段落、表格、图表等版面元素的区域定位；
- 页面级召回和版面级召回的差异。

但 MMDocIR 未必完全适合你要做的 **table-row 粒度消融**。因为你的“表格行级 evidence node”是你自己针对中文年报构建的，而公开数据集未必提供同样细的 row-level gold evidence。

所以 MMDocIR 更适合验证：

> 页面级证据和版面级证据相比，区域级定位是否更有效。

而中文自建数据更适合验证：

> table-block、table-row、paragraph、mixed-node 哪种粒度更适合不同问题类型。

------

## 4. LongDocURL / MMLongBench-Doc 能不能验证结论 1？

只能辅助，不能作为主验证。

它们更适合验证：

- 长文档生成；
- 跨页证据；
- 不可回答问题；
- 跨元素定位；
- 答案是否被证据支持。

你的报告中也把 LongDocURL 或 MMLongBench-Doc 定位为主生成/验证数据集，而不是主检索粒度评估数据集。

所以它们更适合支撑结论 3：

> 加入证据充分性和答案支持性验证后，能降低无依据生成和错误引用。

它们不是验证结论 1 的最佳数据。

------

## 5. 你应该怎么调整实验设计？

我建议你把结论 1 的验证写成这样：

> 结论 1 主要在中文年报/招股书自建数据和 MMDocIR 子集上验证。中文自建数据用于细分 page、paragraph、table-block、table-row、mixed-node 等粒度，并按财务数值题、文本解释题、表格题等类型分析粒度差异；MMDocIR 用于补充验证页面级与版面级证据检索在公开数据集上的泛化效果。LongDocURL 或 MMLongBench-Doc 主要用于后续可信生成、跨页证据和不可回答问题验证。

这样逻辑最稳。

------

## 6. 你需要补充哪些标注？

为了验证结论 1，你的自建数据必须补充这些字段：

| 字段           | 用途                                     |
| -------------- | ---------------------------------------- |
| question_type  | 区分数值题、文本题、表格题、跨页题       |
| gold_page_id   | 评价 Page Recall                         |
| gold_node_id   | 评价 Region Hit / Node Recall            |
| gold_node_type | 判断最佳证据粒度                         |
| gold_answer    | 评价答案正确性                           |
| gold_citation  | 评价引用准确性                           |
| required_slots | 指标、年份、单位、数值或实体、条件、结论 |
| is_answerable  | 支持不可回答问题评价                     |

尤其是 **gold_node_type** 很关键。
没有它，你很难证明“这个问题适合 table-row 还是 paragraph”。

------

## 7. 最终回答：能不能满足？

可以这样说：

> 目前的数据集组合能够支撑结论 1 的初步验证，但还不能充分支撑最终结论。中文年报自建数据最适合验证不同证据粒度的作用，因为它可以构建 page、paragraph、table-block、table-row 等 evidence nodes，并标注 gold evidence；MMDocIR 可以作为公开数据集补充验证页面级和版面级粒度差异。LongDocURL、MMLongBench-Doc 和 ViDoRe V3 更适合验证跨页、生成、不可回答和视觉 grounding，不宜作为粒度消融的主数据集。后续需要扩展中文样本规模、平衡问题类型，并补充 gold_node_type、gold_node_id 和 required_slots 等标注，才能更有力地证明“不同证据粒度适合不同问题类型”。

一句话总结：

> **结论 1 主要靠中文自建数据和 MMDocIR 验证；现在的数据基础够做初步实验，但要支撑论文结论，还需要扩展样本量、细化证据节点标注，并按问题类型分组分析。**