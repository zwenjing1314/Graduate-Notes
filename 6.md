可以把毕业论文的核心从“做一个中文年报问答系统”调整为：

> **面向复杂版面文档的结构感知多模态检索模型研究**

更聚焦的题目可暂定为：

> **面向复杂版面文档检索的查询引导结构感知多模态模型研究**

这里的核心不是调用某个视觉模型，而是研究一个明确问题：

> 现有视觉文档检索模型通常把页面看成大量独立 patch 或整页图像，缺少对标题、段落、表格、图片、多栏结构及阅读顺序的显式建模，导致复杂版面中的关键信息被无关内容干扰。

你的论文目标可以定义为：

```
输入：用户查询 + 文档页面图像 + OCR 文本 + 版面坐标
输出：与查询最相关的文档页面，并给出关键证据区域
目标：在复杂版面、表格密集、多栏文档等场景，
      提升页面检索准确率、证据定位能力和跨数据集泛化能力。
```

你的模型框架可以先称为 `StructDocIR`，后续形成正式名称：

```
查询 q
  │
  ├── 查询编码器
  │
文档页面图像 + OCR 文本 + 坐标
  │
  ├── 视觉编码器
  ├── 文本编码器
  └── 版面区域构建器
          │
          ├── 标题 / 正文 / 表格 / 图片 / 页眉页脚区域
          └── 位置、包含、相邻、阅读顺序关系
                    │
             结构关系交互模块
                    │
          查询引导区域选择模块
                    │
     页面全局匹配 + 区域细粒度匹配 + 结构一致性匹配
                    │
              最终页面检索排序
```

建议的实现方式：

1. 使用已有视觉语言模型作为底座，例如 ColPali 或 ColQwen2 的页面编码能力。
2. 初期冻结大部分视觉与文本编码器，仅训练你新增的区域、结构和交互模块。
3. 后期给底座最后几层加入 LoRA/Adapter，使其适配文档检索，但不需要全参数训练。
4. 用“查询-正确页面”对训练检索模型，采用对比学习损失。
5. 增加区域监督或弱监督：正确页面中的关键文本/表格区域，应比无关区域获得更高匹配分数。
6. 在 `MMDocIR` 上做最终测试；训练集可使用具备 query-page 对的数据集，不能把最终测试数据拿去调参。

损失函数可以初步设计为：

\[ \mathcal{L}= \mathcal{L}_{retrieval} +\lambda_1\mathcal{L}_{region} +\lambda_2\mathcal{L}_{structure} \]

其中：

- \(\mathcal{L}_{retrieval}\)：正确页面的检索分数高于错误页面。
- \(\mathcal{L}_{region}\)：关键区域比无关区域更匹配查询。
- \(\mathcal{L}_{structure}\)：模型学习区域间的版面与阅读顺序关系。

这是一套真正的模型方法，而不是流程图。你需要证明的结论不是“我做了一个系统”，而是：

> 显式的版面结构建模与查询引导区域交互，能够减少复杂页面中无关 patch 的干扰，从而提升多模态文档检索性能。

建议你阅读和复现的论文分为三组。

| 方向         | 论文                                            | 阅读重点                                                 |
| ------------ | ----------------------------------------------- | -------------------------------------------------------- |
| 检索基础     | [ColBERT](https://arxiv.org/abs/2004.12832)     | Late Interaction 的细粒度查询-文档匹配                   |
| 检索强化     | [ColBERTv2](https://arxiv.org/abs/2112.01488)   | 高效检索、蒸馏、训练与索引策略                           |
| 视觉文档检索 | [ColPali](https://arxiv.org/abs/2407.01449)     | 使用页面视觉 patch 进行文档检索，是你的首要复现对象      |
| 视觉文档检索 | ColQwen2                                        | 强力 VLM 文档检索基线，重点研究其多向量表示和训练方式    |
| 视觉文档评测 | ViDoRe: The Visual Document Retrieval Benchmark | 理解视觉文档检索的任务、数据和指标设计                   |
| 文档 RAG     | [VisRAG](https://arxiv.org/abs/2410.10594)      | 检索到页面图像后如何支持下游问答                         |
| 版面建模     | [LayoutLMv3](https://arxiv.org/abs/2204.08387)  | 文本、图像、二维坐标的统一表示                           |
| 版面建模     | [UDOP](https://arxiv.org/abs/2212.02623)        | 文档结构信息如何进入预训练和下游任务                     |
| 布局注意力   | [DocLLM](https://arxiv.org/abs/2401.00908)      | 布局感知注意力机制，最值得借鉴结构设计                   |
| 数据集       | [DocLayNet](https://arxiv.org/abs/2206.01062)   | 标题、表格、图片、正文等版面区域标注，可支持你的区域模块 |
| 高效训练     | [LoRA](https://arxiv.org/abs/2106.09685)        | 在 3080 Ti 上训练视觉语言模型的现实路径                  |

建议优先级是：

```
第一阶段：
ColBERT → ColPali → ColQwen2 → MMDocIR 基线复现

第二阶段：
LayoutLMv3 → DocLLM → UDOP → DocLayNet

第三阶段：
根据 ColPali / ColQwen2 在 MMDocIR 的失败案例，
只选择一种核心问题设计你的新模块。
```

不要同时解决“表格、图片、跨页、多语言、生成问答、长文档”所有问题。最稳妥的研究范围是：

> **复杂版面页面检索，重点关注多栏、表格和图文混排页面。**

原来的中文年报项目也不必完全丢弃。它可以作为毕业论文的领域外验证或应用案例：你的通用模型先在公开文档检索数据上验证，再测试它在中文财务年报上的迁移能力。这样原有工作会成为论文的一部分，而不再承担核心创新。



可以。三个阶段不是简单“读几篇论文”，而是逐步把你从“会运行模型”推进到“能提出并验证自己的模型”。每个阶段都要有明确产出，否则后面容易又变成只改工程代码。

## 第一阶段：理解并复现视觉文档检索基线

目标是回答两个问题：

1. 现在最强的模型到底怎样做页面检索？
2. 它在 `MMDocIR` 的哪些样本上稳定失败？

这一阶段不要提出新模型，也不要改模型结构。

### 重点论文

| 顺序 | 论文                              | 必须弄懂的内容                                               |
| ---- | --------------------------------- | ------------------------------------------------------------ |
| 1    | ColBERT                           | 什么是双编码器、token 级 Late Interaction、为何不是只算一个向量相似度 |
| 2    | ColBERTv2                         | 训练正负样本、蒸馏、索引和效率优化                           |
| 3    | ColPali                           | 如何将整页文档图像编码为视觉 patch 向量，并做细粒度检索      |
| 4    | ColQwen2                          | 强视觉语言模型如何作为文档检索器使用                         |
| 5    | ViDoRe / MMDocIR 的论文或说明文档 | 数据集任务、查询类型、文档类型、评价指标和数据划分           |
| 6    | VisRAG                            | 文档页面检索与下游问答之间的关系                             |

### 每篇论文要做的阅读笔记

不能只记录摘要。每篇都要按相同模板写一页：

```
1. 论文解决的具体问题：
2. 输入和输出：
3. 基础模型：
4. 模型结构中新增或关键的模块：
5. 查询和页面如何编码：
6. 查询与页面如何匹配：
7. 损失函数：
8. 训练数据：
9. 评价指标：
10. 最强结果：
11. 作者自己承认的局限：
12. 我认为可能失败的场景：
```

重点看第 4、6、7、11、12 项，它们将直接决定你的创新点。

### 复现任务

先选一个主基线。建议先复现 ColPali；如果显存、依赖或推理速度不合适，再使用 ColQwen2 的较小版本。

完成标准：

```
输入：MMDocIR 的官方查询和候选文档
输出：每个查询的 Top-K 页面排序结果
指标：Recall@1、Recall@5、Recall@10、MRR@10、nDCG@10
结果：与论文或官方基线的结果进行对比
```

要记录：

```
模型名称与版本
参数量
输入分辨率
是否使用 OCR
最大页面数
batch size
显卡和显存
运行时间
完整命令
代码 commit
```

这一步即使没有复现到论文最高数字也没关系，但必须能够解释差异，例如数据版本不同、模型版本不同、候选池不同、显存限制导致分辨率或 batch size 不同。

### 错误分析任务

将失败查询至少人工分析 100 条，建立表格：

| 失败类型   | 例子                 | 可能原因                    |
| ---------- | -------------------- | --------------------------- |
| 多栏正文   | 相关文字在右栏       | 模型没有理解阅读顺序        |
| 表格页面   | 目标字段在表格行中   | 关键单元格被大量 patch 淹没 |
| 图文混排   | 图注或图片中有答案   | 文本与视觉区域没有区分      |
| 长页面     | 相关内容很小         | 全局视觉信息过于分散        |
| 标题相似   | 多页都含相似关键词   | 模型缺乏细粒度结构约束      |
| 低质量扫描 | OCR 或视觉细节不稳定 | 输入质量影响表征            |

第一阶段的最终产出是：

```
可重复运行的强基线
基线结果表
100 条以上错误案例分类
一份文献笔记
“现有模型在什么场景下失败”的明确结论
```

------

## 第二阶段：学习版面结构建模，确定你的研究问题

目标是回答：

> 现有模型为什么会失败？我增加什么结构，才能有针对性地解决这个失败原因？

这一阶段仍然不要急着训练大模型。先理解“文档结构”可以怎样进入模型。

### 重点论文

| 顺序 | 论文                    | 重点                                            |
| ---- | ----------------------- | ----------------------------------------------- |
| 1    | LayoutLMv3              | 文字、图像、二维坐标怎样联合表示                |
| 2    | UDOP                    | 文本、图像、布局如何统一处理                    |
| 3    | DocLLM                  | 如何用布局感知注意力替代普通位置编码            |
| 4    | DocLayNet               | 文档区域类别和版面检测数据如何使用              |
| 5    | DiT / LayoutParser      | 文档版面分析与区域检测的工程基础                |
| 6    | LoRA / Adapter 相关论文 | 怎样在单张 3080 Ti 上训练新增模块或少量底座参数 |

### 实验与复现任务

这一阶段不要求完整重训 LayoutLMv3 或 DocLLM，因为那会偏离你的目标且计算成本高。你需要复现或验证其中一个“结构提取能力”。

例如：

```
页面图像
  ↓
版面分析模型
  ↓
标题、正文、表格、图片、页眉页脚的区域框
  ↓
区域文本、区域图片、区域坐标、区域类别
```

然后把它接到第一阶段的检索基线后，做一个**不训练的诊断实验**：

1. 检索基线最相似的页面是否真的包含关键区域。
2. 关键区域属于表格、正文还是标题。
3. 错误页面是否只是在全局视觉或标题上相似。
4. 如果只保留关键类别区域，排序是否会变化。
5. 查询类别与关键区域类别是否有关，例如数值查询偏向表格。

这里的目的不是取得最好分数，而是证明你提出的新模块确实有数据依据。

### 阶段末必须形成的内容

你要写出一段明确、可证伪的研究假设。例如：

> 在复杂版面文档中，基于视觉 patch 的检索模型缺少区域边界和区域关系建模，导致关键表格或段落的局部语义被全页无关内容稀释。若将页面构建为带有空间关系的区域图，并使用查询引导的区域交互进行匹配，则能提升复杂版面查询的检索性能。

同时确定范围。建议只选一个核心问题：

```
推荐：表格、多栏和图文混排文档的页面检索
不推荐：同时做多语言、跨页推理、文档生成、OCR 纠错、文档分类和问答
```

第二阶段产出：

```
错误类型与结构因素之间的证据
数据与训练集可用性说明
研究假设
模型输入输出定义
第一版架构图
与现有模型的差异表
```

------

## 第三阶段：实现并验证自己的模型框架

目标是把第二阶段的假设实现为可训练模型，并通过严谨实验说明它有效。

建议先做一个最小可行版本，而不是一次写完整模型。

### 模型的最小版本

```
查询 q
  ↓
预训练查询编码器
  ↓
页面区域 {r1, r2, ..., rn}
  ├─ 区域图像特征
  ├─ OCR 文本特征
  ├─ 区域类别
  └─ 二维坐标
  ↓
区域关系图编码器
  ↓
查询引导区域注意力
  ↓
区域匹配得分 + 页面全局匹配得分
  ↓
最终页面检索得分
```

初始训练策略：

```
冻结：视觉/文本基础编码器的大部分参数
训练：区域图编码器、查询引导模块、匹配头
可选：基础编码器最后几层 LoRA
```

这样你可以将贡献清晰地归到自己的模块上。

### 推荐的逐步实现顺序

1. **Baseline 重现**
   先固定第一阶段最强基线，作为所有对比的统一起点。
2. **区域表示模块**
   将每页表示为若干区域，而不是只使用全页 patch。
3. **查询引导区域选择**
   让查询与不同区域计算相关性，并聚合关键区域。
4. **区域结构关系模块**
   用轻量图网络或布局感知 Transformer，建模上下、左右、包含、同列、阅读顺序关系。
5. **联合排序训练**
   正确页面得分高于负页面，同时让关键区域获得更高相关性。
6. **LoRA 适配实验**
   比较只训练新模块、加 LoRA、解冻末层三种方式。

### 必须完成的实验

| 实验         | 要回答的问题                                            |
| ------------ | ------------------------------------------------------- |
| 主实验       | 你的模型是否优于 BM25、Dense、ColPali/ColQwen2 等基线？ |
| 消融实验     | 区域模块、结构模块、查询引导模块分别有什么作用？        |
| 场景实验     | 在表格、多栏、图文混排子集上是否更有效？                |
| 训练策略实验 | 冻结、LoRA、解冻末层的准确率与成本差异是什么？          |
| 参数实验     | 区域数量、图层数、候选数、损失权重如何影响结果？        |
| 效率实验     | 索引时间、单查询延迟、显存、吞吐量是否可接受？          |
| 泛化实验     | 在未参与训练的数据集或中文年报上是否仍有提升？          |
| 错误分析     | 新模型仍会在哪些文档结构上失败？                        |

核心评价指标保持检索领域标准：

```
Recall@K：正确页面是否进入前 K 个结果。
MRR@K：正确页面排得是否靠前。
nDCG@K：多个相关页面存在时，排序整体是否合理。
Latency / QPS：模型实际检索速度。
GPU Memory：模型训练和推理成本。
```

第三阶段完成的标志不是“模型跑通”，而是你能严谨地写出：

> 在相同训练数据和测试协议下，StructDocIR 在复杂版面子集的 Recall@10 与 nDCG@10 显著高于强视觉文档检索基线。消融结果表明，查询引导区域交互贡献主要增益，而区域关系编码进一步改善多栏与表格页面的检索表现。

最后，你原来的中文年报代码可以用于第三阶段的**迁移实验**：不再作为主线，而是验证你的模型是否能从公开通用文档检索数据迁移到中文财务文档。这样保留已有积累，也让毕业论文的实验更完整。





第一阶段不要同时读很多篇，也不要立刻设计新结构。你现在的第一目标是：**原样复现一个视觉文档检索模型，并弄清它在复杂页面上为什么失败。**

我看了你当前项目的结果：你已经完成的是 BGE-M3 的文本/OCR 页面检索与布局节点检索。当前 `MMDocIR` 实验是“已知问题属于哪份文档，再在该文档内部找页面”，不是跨文档检索；Hybrid 的 `Page R@5=0.7600`，图表类节点定位明显弱于文本类。这正好可以作为后续视觉模型的参照，但不能替代 ColPali 类模型复现。见 [结果记录 (line 1)](/Users/zhouwenjing/Documents/WorkTransfer/PythonProject/mmdoc-evidence-rag/docs/paper/04_mmdocir_results.md:1) 和 [分类型分析 (line 1)](/Users/zhouwenjing/Documents/WorkTransfer/PythonProject/mmdoc-evidence-rag/docs/paper/05_mmdocir_question_type_analysis.md:1)。

第一阶段只读下面五篇，严格按顺序。

| 顺序 | 论文                                                         | 这一阶段必须学会什么                                         |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 0    | [MMDocIR: Benchmarking Multi-Modal Retrieval for Long Documents](https://arxiv.org/abs/2501.08828) | 数据包含什么、查询和金标页如何定义、官方评价协议是什么       |
| 1    | [ColBERT](https://arxiv.org/abs/2004.12832)                  | 为什么检索不只用一个页面向量，而采用 token 级细粒度匹配      |
| 2    | [ColBERTv2](https://arxiv.org/abs/2112.01488)                | 对比学习、难负样本、蒸馏和检索效率                           |
| 3    | [ColPali](https://arxiv.org/abs/2407.01449)                  | 如何把文档页面图像编码为视觉 patch，并用于 Late Interaction 检索 |
| 4    | ColQwen2                                                     | 更强视觉语言模型怎样改造成页面检索器，作为后续强基线         |
| 补充 | [VisRAG](https://arxiv.org/abs/2410.10594)                   | 可在 ColPali 跑通后读，理解检索结果如何进入后续文档问答      |

第一阶段暂时不读 LayoutLMv3、DocLLM、UDOP。这些属于第二阶段。你现在先学会“视觉文档检索模型本身如何训练和匹配”，否则读版面论文容易变成概念堆积。

每篇论文不需要逐字翻译，但必须完成同一份笔记：

```
1. 论文解决的具体问题：
2. 输入和输出：
3. 基础模型：
4. 模型结构中新增或关键的模块：
5. 查询和页面如何编码：
6. 查询与页面如何匹配：
7. 损失函数：
8. 训练数据：
9. 评价指标：
10. 最强结果：
11. 作者自己承认的局限：
12. 我认为可能失败的场景：

任务是什么？
输入是什么？输出是什么？
基础模型是什么？
页面如何切成 token / patch？
查询如何表示？
查询与页面如何计算匹配分数？
训练时的正样本、负样本和损失函数是什么？
用了哪些数据集和指标？
它相比上一代方法改了什么？
作者承认的局限是什么？
我怀疑它在哪种页面上会失败？
```

其中 ColPali 需要额外画出它的模型数据流图，确保你能口头说清：

```
查询文本 -> 查询 token 向量
页面图像 -> 视觉 patch 向量
查询 token 与所有 patch 逐个细粒度匹配
聚合得到页面相关性分数
按分数返回 Top-K 页面
```

复现时，建议先选择 **ColPali**，而不是一开始选择 ColQwen2。原因是 ColPali 是清晰的论文方法源头，便于理解你后续准备修改的模型结构；ColQwen2 可以作为第二个强基线。

复现按以下顺序进行。

1. 新建独立论文分支，例如：

```
git switch -c thesis/structdocir
```

不要在旧的中文年报小论文分支上直接堆新模型。

1. 建立三份固定记录：

```
docs/literature/           论文阅读笔记
docs/reproduction/         复现过程、环境、命令和问题
runs/colpali/              预测结果、日志、配置副本
```

1. 先使用 ColPali 的官方实现或官方推荐实现，**不要先接入你自己的 `mdr` 框架，也不要先改模型结构**。第一步只验证：

```
模型能否加载
单个查询能否编码
单页文档图像能否编码
能否算出查询-页面匹配分数
能否返回 Top-K 页面
```

1. 用 10 到 20 个 MMDocIR 查询做小样本冒烟测试。逐条人工检查：

```
查询内容是什么？
金标页面是什么？
Top-5 页面是什么？
错误页面为什么相似？
页面图像是否正确加载？
```

1. 再运行完整 MMDocIR 评测。必须保持当前项目一致的候选范围，即：

```
已知所属文档
在该文档的全部页面中排序
计算 Page R@1 / R@5 / R@10 / MRR / nDCG
```

这样才能与当前 BGE-M3 的结果公平对比。不能把 ColPali 跑成“全库跨文档检索”，再与当前“文档内检索”结果直接比较。

1. 保存下列复现产物：

```
使用的模型 ID 与 revision
官方代码仓库 commit
CUDA、PyTorch、transformers 版本
显卡、显存、batch size、图像分辨率
完整命令
预测排序文件
评测指标 JSON / CSV
运行日志和总耗时
```

1. 复现完成后，从 ColPali 的错误结果中挑 100 条做分类。优先查看你当前已经较弱的三类：

```
figure/chart
table
multimodal
```

第一阶段的完成标准是：

```
已读并完成 MMDocIR、ColBERT、ColBERTv2、ColPali、ColQwen2 的结构化笔记；
已在同一 MMDocIR 文档内检索协议下跑通 ColPali；
已获得可复现的完整指标与预测文件；
已完成至少 100 条错误案例分类；
能明确说出一个 ColPali 稳定失败、且版面结构可能有帮助解决的场景。
```

完成这些后，再进入第二阶段读 LayoutLMv3、DocLLM 等版面结构论文，并且只针对你在第一阶段发现的那一种失败场景设计自己的模型。

