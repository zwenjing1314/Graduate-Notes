本文所谓粒度评估，是指在同一文档、同一问题和同一检索模型下，对 page、paragraph、table-block、table-row、mixed-node 和 mixed-node+structure 等证据粒度进行消融比较，评估其对检索召回、区域定位、引用准确性和答案支持性的影响。



开题时可以这样回答：

> 方法一的粒度消融主要通过固定检索模型，比较 page-only、paragraph-only、table-block、table-row、mixed-node 和 mixed-node+structure 等不同证据表示，评价其在页面召回、区域定位、引用准确性和答案支持性上的差异，从而确定最终证据库应采用哪些节点类型。方法二会受到方法一粒度选择的影响，因为层次化检索是在这些 evidence nodes 基础上进行的。因此，本文会先通过粒度消融确定较优的证据节点组合，再在该证据库上比较 global-region、predicted-page→region、oracle-page→region、single-node 和 evidence-set 等检索策略，验证层次化检索和问题要素覆盖特征的贡献。

一句话概括：

> **方法一解决“证据库怎么建”，方法二解决“建好之后怎么检索”；证据粒度会影响检索效果，因此粒度消融是后续层次化检索实验的前提。**



本文三个创新点不是并列关系，而是递进关系：第一点解决证据如何组织和评估，第二点解决如何找到足以回答问题的证据集，第三点解决证据是否足够、答案是否被支持以及引用是否一致。其中，第二点“问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法”是本文核心方法。

### 1. 构建面向检索、引用与验证的多粒度证据节点表示与粒度评估方法

针对固定长度文本切块易破坏长文档版面结构、表格关系和证据来源的问题，本文构建多粒度证据节点表示方法。该方法将页面、章节、段落、表格块、表格行和图表区域等文档元素统一建模为 evidence node，并记录页码、区域坐标、元素类型、文本内容、层级关系、阅读顺序、表格归属和来源信息，使文档内容由普通文本片段转化为可定位、可引用和可验证的证据单元。

该创新点的重点不只是保存节点字段，而是通过粒度消融实验分析不同证据粒度的作用。本文将比较 page-only、paragraph-only、table-block、table-row、mixed-node 与 mixed-node+structure 等设置，评估不同粒度对页面召回、区域定位、引用准确性和答案支持性的影响。

### 2. 设计问题要素覆盖驱动的页面—区域—证据集层次化检索与重排序方法

针对长文档页数多、整页检索粒度粗、直接区域检索噪声较大的问题，本文设计问题要素覆盖驱动的层次化证据检索方法。该方法先进行页面级候选召回，再在候选页面内检索段落、表格块、表格行和图表区域等细粒度 evidence nodes，最后结合语义相关性、页面得分、节点类型、版面位置和问题要素覆盖度选择候选 evidence set。

对于财务数值类问题，重点判断候选证据是否覆盖“指标—年份—单位—数值”等要素；对于文本解释类问题，重点判断是否覆盖关键实体、条件和结论。本文所称“充分”是指证据集覆盖回答所需关键要素，“最小”是指在满足证据覆盖的前提下减少冗余页面和节点数量。该方法不只排序单个节点，而是面向可回答性选择最小充分证据集，以提升证据定位能力和下游答案支持性。

实现上，系统将通过 BM25、BGE-M3 或混合检索召回候选页面，再检索页面内 evidence nodes，并融合语义相关性、页面先验、节点类型、版面位置和问题要素覆盖度形成综合得分。后续将通过 page-only、global-region、oracle-page→region、predicted-page→region、single-node vs. evidence-set 和覆盖特征消融验证方法贡献。

### 3. 形成面向证据充分性、答案支持性与引用一致性的分层可信验证机制

针对文档 RAG 中证据不足仍强行回答、答案与证据不一致、引用错误和不可回答问题误答等问题，本文形成分层可信验证机制。首先在生成前判断 evidence set 是否足以回答问题；其次在生成后检查答案关键 claim 是否被证据支持；最后验证引用是否指向正确页码、区域或表格证据。

对于数值和表格型问题，重点进行“指标—年份—单位—数值”一致性校验；对于文本型问题，结合证据覆盖度与 NLI/LLM verifier 判断答案支持性；对于不可回答问题，结合检索置信度阈值和 verifier 执行二次检索或拒答。最终输出 supported、partially_supported、insufficient、conflict、citation_mismatch 和 unanswerable 等状态，以降低无依据生成和错误引用风险。

对于数值和表格型问题，本文从答案和证据中抽取指标、年份、单位和数值，并检查二者是否一致；对于文本型问题，本文结合证据是否覆盖答案中的关键实体、条件和结论，以及 NLI/LLM verifier 的判断结果，验证答案是否被证据支持；对于不可回答问题，本文根据检索分数、证据要素覆盖情况和 verifier 判断结果，决定是否进行二次检索或拒答。



## 我想问一下这个消融实验用的哪些方法证明不同粒度的好坏呢？方法一的不同粒度选取会不会影响到方法二中的检索效果？

需要比较哪些粒度？

| 设置                 | 含义                                              | 主要验证什么                   |
| -------------------- | ------------------------------------------------- | ------------------------------ |
| page-only            | 只用整页作为证据单元                              | 页面级证据是否足够             |
| paragraph-only       | 只用段落作为证据单元                              | 文本解释题是否适合段落粒度     |
| table-block          | 只用表格块作为证据单元                            | 表格整体是否能定位证据         |
| table-row            | 只用表格行作为证据单元                            | 财务数值题是否更适合表格行     |
| mixed-node           | 混合页面、段落、表格块、表格行等节点              | 多类型问题是否需要混合粒度     |
| mixed-node+structure | 混合节点 + 标题层级、表格归属、阅读顺序等结构信息 | 结构信息是否提升检索和引用效果 |

为了公平，方法一的粒度消融最好先使用**固定检索方法**，例如：

- BM25；
- BGE-M3 / bge-large-zh；
- BM25 + Dense 混合检索。

也就是说：

> 不要一开始就让每种粒度配不同模型，否则无法判断效果差异到底来自“粒度”，还是来自“模型”。



| 实验设置                     | 主要验证的问题           |
| ---------------------------- | ------------------------ |
| page-only                    | 页面级证据是否太粗       |
| global-region                | 是否可以直接全局区域检索 |
| oracle-page→region           | 区域定位的理论上限       |
| predicted-page→region        | 真实两阶段检索效果       |
| single-node vs. evidence-set | 单节点是否不如证据集     |
| 覆盖特征消融                 | 问题要素覆盖度是否有效   |

## 所以说你到底研究的是什么？最后要展示的是什么效果？

我围绕长文档问答中的“证据可信性”问题，研究从证据表示、证据检索到证据验证的完整方法。

我的研究重点不是训练新的问答模型，而是研究长文档 RAG 中证据如何被组织、检索和验证。最终目标是让系统在回答问题时，不仅给出答案，还能定位到页码、段落、表格行或图表区域，并判断答案是否被证据支持。最终展示效果是“答案 + 证据位置 + 证据内容 + 支持状态”，并通过实验验证多粒度证据表示、层次化检索和可信验证机制能够提升证据定位准确性、引用准确性和答案可信性。





## 你可以这样给老师解释创新点二

老师，这个方法不是简单把 Page→Region 做一遍。基础流程确实是先召回候选页面，再在候选页面内定位段落、表格行等 evidence nodes。但本文进一步引入问题要素覆盖度，把检索目标从“找最相似节点”改为“选择最小充分证据集”。例如财务数值题需要覆盖指标、年份、单位和数值，文本解释题需要覆盖关键实体、条件和结论。系统会综合页面得分、节点得分、节点类型、版面位置和问题要素覆盖度进行重排序，并选择能够共同支撑答案的一组证据。这样既减少整页检索带来的冗余，又避免单个区域证据不足的问题。它的核心创新在于从相关性排序提升到证据充分性驱动的证据集选择。



你的方法是：

找能共同覆盖问题关键要素、足以支撑答案的最小证据集

**问题要素覆盖判断**
 不只看相关性，还看证据是否覆盖指标、年份、单位、数值等要素。

**证据集选择**
 不只选一个最高分节点，而是选择一组互补证据，减少冗余并保证充分性。

候选 evidence nodes 由页面级召回后的候选页面内部产生。系统首先通过 BM25、BGE-M3 或混合检索召回 Top-K 页面，然后在候选页面内收集段落、表格块、表格行和图表区域等节点，并计算节点与问题的相关性得到候选集合。最终 evidence set 并非直接取全部候选节点，而是根据语义相关性、页面先验、节点类型、版面位置和问题要素覆盖度进行选择。只有当候选节点具有足够相关性，且能够补充回答所需关键要素、减少证据缺失或提升支持性时，才加入 evidence set；若节点冗余或与已有证据冲突，则不加入或标记为 conflict。



## 你的流程应该是这样

离线阶段：
PDF 解析
→ 构建 evidence nodes
→ 建立 BM25-page 索引
→ 建立 BM25-node 索引
→ 用 BGE-M3 编码 page 和 node
→ 建立 page 向量索引
→ 建立 node 向量索引
→ 建立元数据索引

在线查询阶段：
输入问题
→ 查询 BM25-page / BGE-M3-page
→ 召回候选页面
→ 在候选页面内查询 BM25-node / BGE-M3-node
→ 得到候选 evidence nodes
→ 根据相关性、页面得分、节点类型和问题要素覆盖度重排序
→ 选择 evidence set
→ 生成答案并验证

## 1. 你的“多粒度证据节点”与普通 layout-aware chunk 的本质区别是什么？

普通 layout-aware chunk 主要是**按版面结构切分文本**，比如按标题、段落、表格块切分，然后用于检索。

我的多粒度证据节点不是单纯切块，而是面向后续的**检索、引用和验证闭环**设计的证据单元。每个 evidence node 不仅包含文本，还保留页码、区域坐标、节点类型、层级关系、阅读顺序、表格归属和来源信息。它的目标不是只让模型“读到文本”，而是让系统能回答：证据在哪里、属于什么类型、能否被引用、能否支持答案。

所以区别在于：

> layout-aware chunk 主要服务于检索；
>  evidence node 同时服务于检索、定位、引用和可信验证。

我后续还会通过粒度消融比较 page-only、paragraph-only、table-row、mixed-node 等设置，分析不同粒度对页面召回、区域定位、引用准确性和答案支持性的影响。



## evidence sufficiency 和 answer support 的定义分别是什么？

这两个概念必须区分。

**Evidence sufficiency，证据充分性**，发生在生成前。它判断的是：

> 当前检索到的 evidence set 是否足以回答问题？

例如财务数值题中，如果证据集覆盖了指标、年份、单位、数值，就可以认为充分；如果只找到相关页面但没有具体数值或单位，就不充分。

**Answer support，答案支持性**，发生在生成后。它判断的是：

> 生成答案中的 claim 是否能被 evidence set 支持？

## 不可回答问题是如何构造的？人工标注是否可靠？

不可回答问题不能随便编。我的构造方式会分三类：

第一，**文档不存在型**：问题询问文档中没有披露的信息，比如“公司是否披露了 2026 年营业收入预测”。
 第二，**条件不匹配型**：问题中的年份、主体或指标与文档不匹配。
 第三，**证据不足型**：文档有相关主题，但没有足够证据支持具体答案。



*当前年报实验中，索引是运行时在内存中构建的。BM25-page 采用词项统计方式，接近倒排索引思想；Dense-page 预留了 BGE-M3 向量检索接口，但当前离线实验主要使用 TF-IDF fallback；Page→Region 则先进行页面级召回，再在候选页面内部对证据节点进行二次检索和 RRF 融合排序。后续如果扩展到大规模数据，会将页面和证据节点 embedding 持久化到 FAISS、Milvus 或 Chroma 等向量索引中。*