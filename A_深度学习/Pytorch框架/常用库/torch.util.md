# 介绍

`torch.utils` 是 **PyTorch** 提供的一个**工具集合模块**，里面包含了许多辅助功能，用来简化 **数据处理、训练流程、模型保存/加载、可视化** 等常见任务。

它本身不是一个单一功能的库，而是一个**工具包命名空间**，里面按子模块分类。

# 常见子模块

## data

**作用**：数据加载与批处理工具。

**常用功能**：

- `Dataset`：自定义数据集类。
- `DataLoader`：批量加载数据，支持多线程、打乱顺序等。
- `random_split`：随机划分数据集。

