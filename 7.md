````
下面只汇报 ColPali 官方论文复现本身，按实际顺序说明。

## 1. 确定复现目标

复现的不是“重新训练 ColPali”，而是验证官方发布的预训练模型 `vidore/colpali` 能否在官方 ViDoRe V1 测试集上完成页面检索，并得到接近公开结果的指标。

完整链路：

```text
页面图片 + 文本 query
-> ColPali 编码
-> 多向量 MaxSim 打分
-> 页面排序
-> ViDoRe 官方指标
```

模型输出是“页面相关性排序”，不是答案生成，也不是区域定位。

## 2. 获取官方代码

官方仓库下载到：

`C:\Users\WenJing\Documents\WorkTransfer\colpali-official-repro`

后来上传并克隆后的目录是：

`/Users/zhouwenjing/Documents/WorkTransfer/PythonProject/colpali-official-repro-github/colpali-official-repro`

必须用 `git clone` 获得代码，而不是 GitHub 的 ZIP 下载包。原因是官方 `pyproject.toml` 用 Git 元数据生成版本号；ZIP 没有 `.git`，所以最初 `pip install -e .` 出现 `setuptools-scm was unable to detect version`。

有效命令：

```powershell
git clone https://github.com/illuin-tech/colpali.git colpali-official-repro
cd colpali-official-repro
```

- `git clone`：下载源码及 `.git` 版本信息。
- `cd`：进入项目根目录，后续安装和运行都必须在这里。

## 3. 选择运行机器和 Python 环境

最终选择 Windows 3080 Ti 台式机，而非 Mac。

原因：ColPali 的基础模型约 5.44 GB，页面编码和全量 ViDoRe 检索需要 CUDA GPU；RTX 3080 Ti 有 12 GB 显存，适合推理。Mac 适合看代码或轻量测试，不适合正式全量评测。

使用 Python 3.11。最初 Mac 的 Python 3.13 容易遇到依赖兼容问题，因此 Windows 上统一用 Conda 环境。

实际形成两个环境：

```text
colpali
  用于运行官方 ColPali 模型、自己的本地图片检索验证。

vidore-eval
  用于运行 vidore-benchmark 全量指标评测。
```

分成两个环境的原因：`colpali-engine` 需要较新的 `transformers`，而 `vidore-benchmark==5.0.0` 依赖较旧的 `sentence-transformers`。强行放在一个环境里会出现互相冲突。

创建与进入环境的 Windows 命令：

```powershell
conda create -n colpali python=3.11 -y
conda activate colpali
```

```powershell
conda create -n vidore-eval python=3.11 -y
conda activate vidore-eval
```

PowerShell 不能使用 macOS/Linux 的：

```bash
source .venv/bin/activate
```

因为这是 Bash 命令，不是 PowerShell 命令。

## 4. 安装官方代码和 GPU PyTorch

在 `colpali` 环境、官方仓库根目录运行：

```powershell
python -m pip install -e .
```

- `-e` 是 editable install，表示当前源码目录直接作为 Python 包使用。
- 这是根据官方仓库的 `pyproject.toml` 安装 ColPali Engine 及其依赖。

然后安装 CUDA 版 PyTorch：

```powershell
python -m pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
```

- `cu121` 表示 PyTorch 自带 CUDA 12.1 运行时。
- 不要求你的 NVIDIA 驱动恰好是 CUDA 12.1；驱动只要兼容即可。
- 之前显示 `torch: ...+cpu`、`CUDA: False`，说明安装的是 CPU wheel，因此 GPU 不会被模型使用。

安装后验证：

```powershell
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
```

含义：

- 检查 PyTorch 是否正常导入。
- `True` 表示 CUDA 可用。
- 最后一项应显示 `NVIDIA GeForce RTX 3080 Ti`。

注意：在 `(base)` 环境运行检查得到的 DLL 报错，不代表 `vidore-eval` 或 `colpali` 环境出错；必须先 `conda activate` 再运行 Python。

## 5. 下载并理解模型权重

首次运行模型时，Hugging Face 自动下载了两个关键目录：

```text
models--vidore--colpali
models--vidore--colpaligemma-3b-mix-448-base
```

它们不是两个独立参与排序的模型。

| 名称 | 作用 |
| --- | --- |
| `vidore/colpali` | 最终用于复现的 ColPali checkpoint，包含检索任务训练后的权重与配置 |
| `vidore/colpaligemma-3b-mix-448-base` | ColPali 的基础 PaliGemma 视觉语言模型权重，由前者加载 |

缓存一般位于 Windows 用户目录下：

```text
C:\Users\WenJing\.cache\huggingface\hub\
```

模型和数据缓存很大，不提交 Git。

## 6. 本地小规模检索验证

在官方仓库中创建了：

```text
local_eval/
├── local_retrieval.py
└── pages/
    ├── 1.png
    ├── ...
    └── 8.png
```

文件用途：

| 文件/目录 | 作用 |
| --- | --- |
| `local_eval/pages/*.png` | 你准备的 8 张文档页面图片 |
| `local_eval/local_retrieval.py` | 用这 8 页运行 ColPali，检查模型、GPU、图片输入、MaxSim 排序是否正常 |

脚本的核心步骤是：

```python
model = ColPali.from_pretrained("vidore/colpali", ...)
processor = ColPaliProcessor.from_pretrained("vidore/colpali")
```

加载模型与预处理器。

```python
batch = processor.process_images([image])
embedding = model(**batch)
```

将每页图片编码为多向量视觉特征。3080 Ti 只有 12 GB 显存，所以脚本一次只编码一页，避免显存溢出。

```python
query_embeddings = model(**processor.process_queries(QUERIES))
scores = processor.score_multi_vector(query_embeddings, document_embeddings)
```

将 query 编码为多向量，然后用官方 `score_multi_vector` 实现 MaxSim，得到每个 query 对每页的分数与 Top-5 排名。

运行命令：

```powershell
conda activate colpali
python local_eval\local_retrieval.py
```

你得到的三个 query 都返回了页面排序。这一步的目的只是验证模型链路，不是论文指标复现，因为 8 页自建图片没有官方 benchmark 标注与统一指标。

## 7. 安装 ViDoRe 官方评测工具

在单独的 `vidore-eval` 环境安装：

```powershell
conda activate vidore-eval
python -m pip install vidore-benchmark==5.0.0
```

验证 CLI：

```powershell
vidore-benchmark --help
vidore-benchmark evaluate-retriever --help
```

这些命令不是凭空写的，而是先查看官方工具的 `--help`，得到所需参数：

- `--model-class`：使用哪种检索器实现；
- `--model-name`：Hugging Face 模型名；
- `--dataset-format qa`：ViDoRe V1 的 QA 数据格式；
- `--dataset-name`：运行一个数据集；
- `--collection-name`：运行一组数据集；
- `--batch-*`：控制显存占用；
- `--output-dir`：保存评测 JSON。

`rg` 在 Windows 未安装导致命令找不到，不影响复现本身；只是搜索源码的工具缺失。

## 8. 找到官方 ViDoRe V1 数据集集合

使用 Hugging Face API 查询到的 collection 是：

```text
vidore/vidore-benchmark-667173f98e70a1c0fa4db00d
```

它包含 10 个测试集：

```text
ArXivQA、DocVQA、InfoVQA、TabFQuAD、TAT-DQA、ShiftProject、
SyntheticDocQA-AI、Energy、Government Reports、Healthcare Industry
```

运行命令时，`vidore-benchmark` 会自动从 Hugging Face 下载页面图片、query、相关性标注，并缓存到本地。

下载初期 GPU 利用率低、CPU 较高是正常的：此时主要在网络下载、解压、图片读取和数据预处理；进入模型前向计算后，`nvidia-smi` 才显示 `python.exe` 和较高显存占用。

## 9. 单数据集正式验证

先运行一个数据集，验证 benchmark、模型和显存都能工作：

```powershell
conda activate vidore-eval

vidore-benchmark evaluate-retriever `
  --model-class colpali `
  --model-name vidore/colpali `
  --dataset-format qa `
  --dataset-name vidore/docvqa_test_subsampled `
  --split test `
  --batch-query 1 `
  --batch-passage 1 `
  --batch-score 1 `
  --num-workers 0 `
  --output-dir outputs\colpali_docvqa
```

参数来源和作用：

| 参数 | 来源/作用 |
| --- | --- |
| `colpali` | `vidore-benchmark` 支持的模型类别 |
| `vidore/colpali` | 官方 README 指定的论文 checkpoint |
| `qa` | ViDoRe V1 的数据格式 |
| `docvqa_test_subsampled` | ViDoRe collection 中的一个测试集 |
| `test` | 使用测试切分 |
| 三个 batch 均为 `1` | 为 12 GB 显存设置，降低 OOM 风险 |
| `num-workers 0` | Windows 下避免 DataLoader 多进程问题 |
| `output-dir` | 保存 JSON 结果 |

## 10. ViDoRe V1 全量评测

单数据集成功后，运行整个官方 collection：

```powershell
conda activate vidore-eval

vidore-benchmark evaluate-retriever `
  --model-class colpali `
  --model-name vidore/colpali `
  --dataset-format qa `
  --collection-name vidore/vidore-benchmark-667173f98e70a1c0fa4db00d `
  --split test `
  --batch-query 1 `
  --batch-passage 1 `
  --batch-score 1 `
  --num-workers 0 `
  --output-dir outputs\colpali_full
```

与单数据集命令的唯一核心区别是：

```text
--dataset-name
```

改为：

```text
--collection-name
```

因此它会依次运行完整 10 个数据集。batch 为 1 导致运行很慢，长时间停在某个数据集通常是页面编码或 MaxSim 打分，并非必然卡死。你后来用 `nvidia-smi` 确认了 `vidore-eval\python.exe` 占用约 8.7 GB 显存，说明 CUDA 推理确实在运行。

## 11. 输出文件与最终结果

全量结果目录：

```text
outputs/
└── colpali_full/
    └── colpali_vidore_colpali/
        ├── vidore_arxivqa_test_subsampled_metrics.json
        ├── ...
        ├── vidore_syntheticDocQA_healthcare_industry_test_metrics.json
        └── colpali_vidore_colpali_metrics.json
```

文件用途：

| 文件 | 作用 |
| --- | --- |
| 每个 `*_metrics.json` | 一个 ViDoRe 子数据集的检索指标 |
| `colpali_vidore_colpali_metrics.json` | 10 个数据集的总汇总结果、版本与元数据 |
| `outputs/` | 运行产物，不应作为代码提交 Git |

最终 10 项 `nDCG@5` 为：

```text
78.433, 57.003, 80.563, 84.023, 64.582,
68.901, 93.393, 90.647, 90.701, 91.710
```

宏平均：

```text
80.00%
```

官方 README 公开值是 `81.3`。本次可得出的结论是：在当前 ViDoRe V1 collection、`vidore-benchmark==5.0.0` 和当前依赖版本下，官方 `vidore/colpali` checkpoint 已完成端到端 GPU 推理与 10 数据集评测复现，宏平均 `nDCG@5 = 80.00%`。不能把 1.3 个百分点差异直接称为复现失败，因为官方发布时的数据快照、代码提交和依赖版本未完全锁定。

复现报告已保存于 [colpali_vidore_v1_reproduction_20260830.md](/Users/zhouwenjing/Documents/WorkTransfer/PythonProject/mmdoc-evidence-rag/docs/reproduction/colpali_vidore_v1_reproduction_20260830.md)。
````



````
# 从 ColPali 复现到毕业论文：分阶段研究路线

## 1. 本文档回答什么

本毕业论文不是“复现 ColPali”，也不是现在就确定一个名为 StructDocIR 的最终网络。

当前研究按四个阶段推进：先完成 ColPali 的严格复现并真正理解其方法；再阅读后续相关论文，整理可借鉴的表示、匹配、结构建模和训练方法；在证据支持下形成一篇范围可控的小论文；最后将可靠的方法、实验和分析扩展为毕业论文。

当前的候选方向是：以 ColPali 为视觉页面检索基线，利用 MMDocIR 的页面和布局区域标注，考察结构感知的多粒度检索是否能改善页面检索后的证据定位。它是一个待验证方向，不是已经确定的最终结论。

本文档将以下三类内容严格分开：

| 类别 | 含义 |
| --- | --- |
| 已完成事实 | 已运行、已记录、可复查的工作 |
| 架构事实 | 从 ColPali 原始设计可以直接得出的任务边界 |
| 研究假设 | 需要通过 MMDocIR 实验验证，不能预先当作结论 |

## 2. 总体目标与阶段产出

```text
阶段一：ColPali 原始复现与理解
  -> 可复查的官方结果、复现报告、方法流程图、误差初步观察

阶段二：后续论文阅读与研究设计
  -> 文献矩阵、候选问题、可用模块/损失/训练策略、最小实验方案

阶段三：小论文
  -> 一个边界清楚的研究问题、一套方法、对比/消融/案例分析、可投稿初稿

阶段四：毕业论文
  -> 在小论文基础上扩展任务、实验、分析与系统性论述
```

阶段之间不是机械串行：文献阅读可以从现在开始，但**最终网络、训练损失和论文结论必须在阶段一的复现理解与阶段二的证据诊断之后确定**。

## 3. 阶段一：已完成的 ColPali 复现是什么

已完成的是 ColPali **预训练模型的推理和评测复现**，不包括从头训练。

```text
官方代码 + vidore/colpali checkpoint
+ ViDoRe V1 测试集 + 官方评测工具
-> 页面检索结果与 nDCG@5 等指标
```

已获得的事实：

1. `vidore/colpali` 能在 RTX 3080 Ti 上完成页面图片编码、query 编码和 MaxSim 打分。
2. 已在 ViDoRe V1 的 10 个测试集完成全量评测，宏平均 nDCG@5 为 80.00%。
3. 该结果接近官方 README 列出的 81.3；具体环境、命令和分项指标已记录在 `docs/reproduction/`。

这一步的意义不是获得自己的创新，而是确认以下基础能力可用：

```text
页面图片 -> 多个视觉 patch 向量
query 文本 -> 多个文本 token 向量
MaxSim -> 页面相关性排序
```

因此，ColPali 是本研究的**视觉页面检索 baseline 和候选视觉主干**。

## 4. 阶段一还需要完成什么：从“跑通”到“理解”

目前已经完成了官方 checkpoint 的推理与 ViDoRe V1 指标复现；这说明环境、权重、数据下载和官方评测链路正常。要完成“ColPali 论文复现”这一阶段，还需补齐下列可写入报告的内容：

| 工作 | 要回答的问题 | 产出 |
| --- | --- | --- |
| 方法复盘 | 图像、query、多向量、MaxSim 如何计算？哪些参数训练，哪些冻结？ | 准确的网络/数据流图与公式说明 |
| 实验设置核对 | 模型版本、数据集版本、指标、批大小、GPU、缓存位置是什么？ | 可复跑命令与环境记录 |
| 指标对照 | 与官方数字为何相近或有差异？ | 分数据集结果表与差异解释 |
| 定性案例 | 模型为什么找对或找错页面？ | 至少若干 query 的 Top-K 页面可视化分析 |
| 主项目基线接入 | 在同一 MMDocIR 数据层和评测层中运行 ColPali | 与 BM25/Dense/Hybrid 可比较的结果 |

“复现完成”的标准不是只得到一个 80.00% 宏平均数，而是能够清楚解释模型在做什么、官方结果如何得到、它在自己的任务中表现在哪里有效或不足。

## 5. 阶段二：后续论文阅读如何服务研究设计

后续阅读不以“再找一个更强模型”为唯一目标，而是为可能的研究模块寻找依据。每篇论文均应填写统一阅读卡：任务与数据、输入粒度、表示方式、匹配函数、训练目标/损失、监督信号、消融结论、可迁移点与不适用条件。

优先关注四类问题：

| 阅读方向 | 需要寻找的内容 | 对后续方法的作用 |
| --- | --- | --- |
| 视觉文档检索 | 页面/patch/多向量表示、Late Interaction、索引与重排序 | 视觉主干与匹配设计 |
| 文档结构建模 | layout box、阅读顺序、区域类型、图结构或区域池化 | 让页面 patch 与结构区域建立联系 |
| 视觉-文本融合 | OCR、VLM 描述、跨模态对齐 | 判断何时需要文本补充视觉证据 |
| 检索训练与损失 | 对比损失、in-batch negatives、hard negative、多任务/排序损失 | 仅在有独立训练数据后选择训练方案 |

阶段二的正确产出是 2--3 个可证伪的候选方案，而不是一开始堆叠所有模块。候选模块、损失函数和训练方式应当由论文证据与 MMDocIR 诊断结果共同决定。

## 6. 从阶段二到小论文、大论文

小论文应聚焦一个小而完整的问题，例如“布局区域聚合是否能提升视觉文档检索的证据定位”。其最低完整结构为：

```text
明确问题与假设
-> 可复现 baseline
-> 一个核心方法改动
-> 公平对比、消融与错误案例
-> 有边界的结论
```

毕业论文不必推翻小论文。它可以在小论文的核心方法上扩展：更多数据/任务设置、更完整的区域结构建模、OCR/VLM 融合、训练策略、效率分析、系统实现，以及更全面的定性分析。只有小论文的核心假设经实验支持，才值得作为毕业论文的主线。

## 7. ColPali 给出的启发与任务边界

ColPali 证明：不必先把文档完全转为 OCR 文本；页面图片本身可用于检索，并能保留表格、图像和排版等视觉信息。

但其原始任务单位是“页面”。对于一个 query 和一个 page，模型输出一个页级相关性分数：

```text
query + page image -> one page score
```

原始 ColPali 的视觉 patch 会平铺参与 MaxSim。它没有显式建模：

```text
哪些 patch 属于同一个段落、表格或图像？
哪个区域才是支持答案的证据？
区域的类型、坐标、面积和阅读顺序是什么？
区域的 OCR 或 VLM 文本应怎样参与检索？
```

这不是说 ColPali “做错了”，而是其原始论文要解决的是视觉**页面检索**，并未把“结构化区域证据定位”作为核心输出任务。

## 8. 待验证的问题

本论文的候选研究问题是：

> 在长文档检索中，视觉页面检索虽然可以召回包含答案的页面，但无结构的 patch 级匹配可能无法稳定区分页面内真正的证据区域与标题、页眉、其他相似数字或无关表格。能否利用已有布局节点，把视觉 patch 组织为具有语义边界的区域表示，从而同时提高页面检索和区域级证据定位？

尚未验证的研究假设包括：

| 编号 | 假设 | 需要怎样验证 |
| --- | --- | --- |
| H1 | ColPali 在 MMDocIR 中存在“正确页 Top-K 命中，但正确区域未有效定位”的案例 | 对照页面 gold 和 layout-region gold |
| H2 | 将视觉 patch 按 layout box 聚合为区域向量，能改善区域级检索 | ColPali 与区域聚合版本对比 |
| H3 | 区域类型和空间位置等结构信息能进一步减少干扰区域 | 加/去结构编码的消融实验 |
| H4 | OCR/VLM 区域文本可补足纯视觉表示的细粒度语义 | 加/去区域文本融合的消融实验 |

若 H1 不成立，或后续模块不能带来稳定收益，就必须修改研究主张；不能为了预设网络强行解释结果。

## 9. 为什么用 MMDocIR 验证

ViDoRe V1 用于复现 ColPali 的原始页面检索能力，但它无法充分验证“页面内哪个区域是证据”。MMDocIR 同时提供以下信息：

| MMDocIR 信息 | 在本论文中的作用 |
| --- | --- |
| 长文档与多页页面库 | 验证长文档页面检索 |
| 页面图片 | 保留视觉检索输入 |
| 页面 gold | 评价是否找对页面 |
| layout box 与区域类型 | 定义段落、表格、图像等候选证据区域 |
| layout-region gold | 评价是否定位到正确证据区域 |
| OCR/VLM 页面或节点文本 | 提供区域语义增强候选信息 |
| 问题类型和文档领域 | 分析文本、表格、图像、布局、跨页问题的差异 |

因此，MMDocIR 与 ColPali 的关联是：

```text
ColPali 提供页级视觉检索能力。
MMDocIR 提供检验页级检索之后能否找到区域证据的标注。
```

MMDocIR 不用于证明 ColPali 论文有错误，而是用于证明：在更复杂的长文档证据检索任务中，原始页级视觉检索是否存在可由结构信息弥补的缺口。

## 10. 预期错误案例是什么

以下是需要从 MMDocIR 实验中统计和展示的四类现象：

| 类型 | 页面检索 | 区域定位 | 对研究的意义 |
| --- | --- | --- | --- |
| A | 正确页命中 | 正确区域命中 | ColPali 已足够的样例 |
| B | 正确页命中 | 正确区域未命中 | 结构区域模块最直接的动机 |
| C | 正确页未命中，但文本节点方法命中 | 区域可能命中 | 说明 OCR/VLM 文本可能需要参与融合 |
| D | 页面和区域均未命中 | 均失败 | 需要分析 query、视觉质量或跨页证据问题 |

尤其是 B 类案例。例如一个问题的 gold 是“第 12 页中间的财务表格”，ColPali 可能将第 12 页排入 Top-5，但该页的分数由标题、页眉、其他数字或相似表格 patch 驱动。原始模型无法指出真正的表格区域，也无法用区域 gold 衡量这个缺口。

## 11. 候选方法：结构引导的分层多粒度检索

以下是候选设计，不是已经证明有效的最终网络。

```text
query
  -> query token vectors Q

page image
  -> frozen ColPali patch vectors P

layout boxes
  -> Patch-to-Region Aggregation
  -> region visual vectors R

region type / bbox / reading order
  -> Structure Encoding
  -> structure-aware vectors R'

optional OCR / VLM node text
  -> Region Semantic Fusion
  -> enriched vectors R''

page-level MaxSim(Q, P)
region-level MaxSim(Q, R'')
  -> Page-Region Hierarchical Fusion
  -> ranked pages + ranked evidence regions
```

信息不是一次性拼接输入，而是各司其职：

| 输入 | 作用 |
| --- | --- |
| 页面图片 | 表格线、字体、图像、颜色、相对位置等视觉证据 |
| ColPali patch | 页级视觉多向量基础表示 |
| layout box | 将无结构 patch 对齐为段落、表格、图像等区域 |
| 节点类型、坐标、顺序 | 建模区域结构关系 |
| OCR 文本 | 精确文字语义补充 |
| VLM 文本 | 图像或表格等非纯文字区域的语义补充 |

第一版只应实现最小链路：

```text
冻结 ColPali
-> patch 按 layout box 聚合为 region vector
-> query-region MaxSim
-> 融合页级和区域级分数
```

OCR/VLM 融合、训练型结构编码和更强 VLM 主干都应在最小链路的诊断结果出来后再决定。

## 12. 实验如何证明研究主张

### 8.1 基线诊断

在 MMDocIR 的相同 query、相同候选页面、相同 gold 下比较：

```text
BM25-page
BGE-M3 Dense-page
Hybrid-page
ColPali-page
```

输出：页级指标、区域命中统计、四类错误案例，以及按文本/表格/图像/布局/跨页问题分组的结果。

### 8.2 方法比较

| 实验组 | 要证明什么 |
| --- | --- |
| ColPali | 原始视觉页级 baseline |
| ColPali + Region Aggregation | layout box 聚合是否有效 |
| + Structure Encoding | 坐标、类型、顺序是否有效 |
| + Page-Region Fusion | 两层分数融合是否有效 |
| + OCR/VLM Fusion | 区域文本是否提供额外价值 |
| Full model | 组合效果与代价 |

### 8.3 指标

| 层级 | 核心指标 |
| --- | --- |
| 页面级 | Recall@1、Recall@5、nDCG@5、MRR |
| 区域级 | Region Recall@1、Region Recall@5、Region MRR |
| 证据集 | 多页/多区域问题的覆盖率或现有 sufficiency 指标 |
| 分组分析 | 文本、表格、图像、布局、跨页、不同领域 |

可接受的研究结果不只有“所有指标都提高”：

```text
页级和区域级均提升：支持完整主张。
页级持平、区域级提升：支持“改善证据定位”的主张。
仅表格/图像类提升：形成有边界的多模态场景结论。
无稳定提升：修改模块或缩小研究问题。
```

## 13. 数据与训练边界

当前本地 MMDocIR 是 evaluation set。不能用同一批 gold 标注训练、调模块参数，再将同一批数据作为最终测试结果。

因此第一版优先采取：

```text
冻结 ColPali + 无训练或固定规则的结构区域聚合与分数融合。
```

若后续训练轻量结构模块，必须先获得独立训练来源或建立严格的文档级 train/dev/test 划分，并将训练、模型选择和最终测试彻底隔离。

## 14. 当前下一步

现在不开始写完整自定义网络，也不急于加入 ColQwen2。按以下顺序推进：

1. 在 Windows GPU 机器上跑通主项目中的 ColPali MMDocIR smoke test。
2. 用相同数据设置获得 BGE-M3、Hybrid、ColPali 的页级结果。
3. 基于页面与区域 gold，统计 A/B/C/D 四类错误案例。
4. 只有确认 B 类或 C 类错误有稳定比例后，确定自己的最小结构模块。
5. 完成相关论文的结构化阅读，基于诊断结果列出少量候选模块、损失与训练条件。
6. 再画正式网络图、定义打分公式和消融实验，收敛为小论文方案。

这条路线的核心不是先凑出一个复杂网络，而是让每个模块都对应一个可观察、可量化的 ColPali 任务缺口。

````

