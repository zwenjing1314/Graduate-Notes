创建 conda 环境

```bash
conda create -n papeer_reader python=3.11 -y
```

创建项目目录

```bash
mkdir paper-reader
cd paper-reader
mkdir -p src/paper_reader tests
touch src/paper_reader/__init__.py
```

```
pip install -r requirements.txt
```

