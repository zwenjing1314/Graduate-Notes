```

```



在论文分支中运行

```
cd /Users/zhouwenjing/Documents/WorkTransfer/PythonProject/mmdoc-evidence-rag

UV_CACHE_DIR=.uv-cache \
uv run mdr prepare --dataset cn_annual_reports --limit-docs 20
```

重新从现有 PDF 和 `qa_annotations_v2_reviewed.csv` 构建统一的：

```py
data/processed/cn_annual_reports/documents.parquet
data/processed/cn_annual_reports/pages.parquet
data/processed/cn_annual_reports/nodes.parquet
data/processed/cn_annual_reports/queries.parquet
```

接下来先确认 BGE 模型能被当前项目环境加载。运行：

```shell
cd /Users/zhouwenjing/Documents/WorkTransfer/PythonProject/mmdoc-evidence-rag

HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run python -c "from sentence_transformers import SentenceTransformer; m = SentenceTransformer('BAAI/bge-small-zh-v1.5'); print('模型加载成功'); print('向量维度:', m.get_sentence_embedding_dimension()); print('最大长度:', m.max_seq_length)"
```

预期输出：

```
模型加载成功
向量维度: 512
最大长度: ...
```

然后运行项目测试：

```shell
UV_CACHE_DIR=.uv-cache uv run pytest
```

这两步都通过后，再开始跑第一个 baseline：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_bm25_page.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_bm25_page/latest
```

M25 不需要 BGE，先跑它是为了确认完整的“检索 -> 评估”流程正常。之后再运行 `Dense-page`，验证 BGE 确实参与实验。

BM25 baseline 已完成，实际运行目录是：

```
runs/retrieval/cn_bm25_page/20260814_223403
```

它的关键输出为：

```shell
actual_retrievers: bm25
hits: 1600
Page Recall@1:  0.0750
Page Recall@5:  0.2500
Page Recall@10: 0.3750
MRR:            0.1589
nDCG@5:         0.1693
nDCG@10:        0.2097
Region Hit@5:   0.0000
```

`hits=1600` 是 `160` 个问题各返回最多 `10` 个页面。`Region Hit@5=0` 对页面级方法是正常的，因为它不输出区域节点。

接下来运行 `Dense-page`，这是第一次正式验证 BGE 模型是否真正参与检索：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_dense_page.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_dense_page/latest
```

成功时，第一个命令最后应出现：

```shell
Retrieval run written to: .../runs/retrieval/cn_dense_page/时间戳
```

第二个命令会输出 `Evaluation Metrics` 表格。随后必须检查实际后端：

```shell
cat runs/retrieval/cn_dense_page/latest/run_info.json
```

其中应包含：

```shell
"actual_retrievers": [
  "dense:sentence_transformers:BAAI/bge-small-zh-v1.5:maxlen=128"
]
```

这行才证明它用的是 BGE 模型。若出现 `dense:tfidf_fallback`，则说明没有真正使用模型；但当前配置启用了 `require_model: true`，正常情况下模型加载失败会直接报错，不会产生这种结果。之后我会继续给出每个命令的用途、成功输出和指标解读。

Dense-page 已确认真正使用了 BGE：

```shell
dense:sentence_transformers:BAAI/bge-small-zh-v1.5:maxlen=128
```

本轮结果：

```shell
Page Recall@1:  0.1000
Page Recall@5:  0.2688
Page Recall@10: 0.3688
MRR:            0.1698
nDCG@5:         0.1838
nDCG@10:        0.2165
```

与 BM25 相比，Dense 的 `Recall@1/@5`、`MRR`、`nDCG` 更高，但 `Recall@10` 略低。这是正常现象，说明 BGE 更擅长把正确页面排到前面，但和关键词检索仍有互补空间。

下一步运行 `Hybrid-page`，即 BM25 与 BGE 的页面结果做 RRF 融合：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_hybrid_page.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_hybrid_page/latest
```

第一个命令成功时会输出：

```shell
Retrieval run written to: .../runs/retrieval/cn_hybrid_page/时间戳
```

第二个命令输出 `Evaluation Metrics` 表。`run_info.json` 中会显示 `actual_retrievers: hybrid_page`；该配置也有 `require_model: true`，且已验证 Dense-page 正确使用 BGE，因此本次融合中的 dense 分支可靠。

完成后，把 Hybrid-page 的指标发给我或直接告诉我运行完成；下一步会开始 `Page -> Region`，从“找到正确页面”进入“定位具体段落或表格行”。

Hybrid-page 已完成，结果如下：

```shell
Page Recall@1:  0.0688
Page Recall@5:  0.3063
Page Recall@10: 0.5250
MRR:            0.1741
nDCG@5:         0.1856
nDCG@10:        0.2548
```

它的 `Page Recall@10=0.5250`，高于 BM25 的 `0.3750` 和 Dense 的 `0.3688`，说明 RRF 融合有效扩大了正确页面的候选覆盖。`Recall@1` 较低不代表方法失败，因为本实验的目标是为下一阶段提供更完整的候选页面。

下一步运行第一个细粒度证据定位 baseline：`Page -> Region`。

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_page_region.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_page_region/latest
```

它的流程是：

```
问题 -> BGE 检索 Top-10 页面 -> 在这些页面的 paragraph / table_block / table_row 中用 BGE 检索 Top-5 节点
```

成功时，第一个命令会输出新的运行目录；第二个命令会输出页面指标和最重要的：

```shell
Region Hit@5
```

`Region Hit@5` 表示前 5 个返回节点中，是否至少包含一个标注的正确证据节点。它是后续衡量 Evidence Set 方法是否有效的核心 baseline。

`Page -> Region` 已完成，结果是：

```
Page Recall@1:  0.0750
Page Recall@5:  0.2313
Page Recall@10: 0.2313
MRR:            0.1088
nDCG@5:         0.0803
Region Hit@5:   0.1813
```

这里最重要的是：

```
Region Hit@5 = 0.1813
```

说明简单的“两阶段检索”已经能够命中约 18.13% 问题的正确证据节点，但页面候选阶段仍然会漏掉不少正确页面。

下一步运行 `Hybrid-Page -> Region`：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_hybrid_page_region.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_hybrid_page_region/latest
```

它的流程是：

```
问题
 -> BM25 页面召回
 -> BGE 页面召回
 -> RRF 融合页面
 -> 在融合页面中进行 BGE 区域检索
```

成功后重点记录：

```
page_recall@10
region_hit@5
mrr
ndcg@5
```

同时检查：

```shell
cat runs/retrieval/cn_hybrid_page_region/latest/run_info.json
```

预期：

```
"retriever_type": "hybrid_page_region"
```

本次返回数量通常为：

```
hits = 160 × 5 = 800
```

完成后，我们再运行 `Global-Region`，用来判断“不经过页面召回、直接检索全文节点”是否能弥补页面阶段的漏召回。

`Hybrid-Page -> Region` 已完成：

```
Page Recall@1:  0.0500
Page Recall@5:  0.3250
Page Recall@10: 0.3250
MRR:            0.0645
nDCG@5:         0.0507
Region Hit@5:   0.1813
```

与上一轮相比：

```
Page Recall@10: 0.2313
Region Hit@5:   0.1813 -> 0.1813
```

这说明 BM25+BGE 融合确实增加了正确页面候选，但区域阶段没有因此命中更多正确节点。当前瓶颈可能在区域节点排序，而不只是页面召回。

下一步运行 `Global-Region`：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_global_region.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_global_region/latest
```

它跳过页面阶段，直接在所属年报的全部区域节点中检索：

```
问题 -> 全文 paragraph / table_block / table_row -> BGE Top-5 节点
```

需要重点比较：

```shell
Global-Region Region Hit@5
vs
Page -> Region Region Hit@5
vs
Hybrid-Page -> Region Region Hit@5
```

如果 Global-Region 更高，说明页面过滤造成了漏召回；如果仍然较低，说明主要问题在区域节点表示或排序。

`Global-Region` 已完成：

```shell
Page Recall@1:  0.1250
Page Recall@5:  0.2688
MRR:            0.1761
nDCG@5:         0.1210
Region Hit@5:   0.2625
```

关键结论：

```shell
Page -> Region:          Region Hit@5 = 0.1813
Hybrid-Page -> Region:   Region Hit@5 = 0.1813
Global-Region:            Region Hit@5 = 0.2625
```

直接在全文节点中检索比两阶段方法更好，说明当前主要问题确实是页面候选过滤造成的漏召回。这为 Evidence Set 方法合并多来源候选提供了实验动机。

下一步运行本论文的核心方法 `Evidence Set Region`：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_evidence_set_region.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_evidence_set_region/latest
```

方法流程是：

```
Hybrid-page 页面候选
+ Global-Region 节点候选
+ 结构化数值扫描
+ 首页锚点
-> 合并去重候选池
-> 槽位覆盖与结构化打分
-> 贪心选择最多 3 个证据节点
-> 输出 Top-5
```

完成后还要运行证据充分性评价：

```shell
UV_CACHE_DIR=.uv-cache \
uv run mdr verify-evidence \
  --run runs/retrieval/cn_evidence_set_region/latest \
  --top-k 5
```

它会额外生成：

```
evidence_sufficiency_metrics.json
evidence_sufficiency_cases.csv
evidence_sufficiency_summary.md
```

这一步的重点不只是 `Region Hit@5`，还要记录：

```shell
sufficiency_rate
partial_or_sufficient_rate
citation_mismatch_rate
avg_required_item_coverage
```

这是小论文从“检索命中”过渡到“证据是否足够支撑答案”的核心实验。

Evidence Set 的检索和评估已完成，核心结果非常明显：

```
Page Recall@5:  0.8313
Region Hit@5:   0.8313
MRR:            0.5526
nDCG@5:         0.4379
```

与 `Global-Region` 的 `Region Hit@5=0.2625` 相比，Evidence Set 达到 `0.8313`。这说明多来源候选池、数值扫描、槽位覆盖和证据集选择的组合有显著效果，是当前小论文最核心的主结果。

但我发现本轮还没有生成“证据充分性”文件，因此下一步先运行：

```shell
UV_CACHE_DIR=.uv-cache \
uv run mdr verify-evidence \
  --run runs/retrieval/cn_evidence_set_region/latest \
  --top-k 5
```

成功时会输出标题为：

```
Evidence Sufficiency Metrics
```

并在该实验目录生成：

```
evidence_sufficiency_metrics.json
evidence_sufficiency_cases.csv
evidence_sufficiency_summary.md
```

重点记录：

```
sufficiency_rate
partial_or_sufficient_rate
citation_mismatch_rate
avg_required_item_coverage
```

完成这一步后，再运行 `Oracle-Page -> Region`。它使用正确证据页作为输入，是区域检索的诊断上界，用于说明当前普通页面召回与理想候选页之间的差距。

充分性评价已完成，结果如下：

```
Sufficiency Rate:              0.8125  (130 / 160)
Partial or Sufficient Rate:    0.8938  (143 / 160)
Citation Mismatch Rate:        0.1000  (16 / 160)
Average Item Coverage:         0.9734
Region Hit@5:                  0.8313
```

含义是：130 个问题的 Top-5 证据同时覆盖所需信息并命中标注引用；13 个问题部分覆盖；16 个问题虽然包含指标、年份、单位和数值，但没有命中标注的 gold 节点，需要后续区分“标注不唯一”与“实际错引”。

下一步运行 Oracle-Page -> Region：

```shell
HF_HOME=artifacts/hf_cache \
UV_CACHE_DIR=.uv-cache \
uv run mdr retrieve --config configs/experiments/cn_oracle_page_region.yaml

UV_CACHE_DIR=.uv-cache \
uv run mdr evaluate --run runs/retrieval/cn_oracle_page_region/latest
```

它的流程是：

```
问题 -> 直接使用标注的正确证据页 -> 在正确页中用 BGE 检索 Top-5 节点
```

成功时会生成新运行目录，评估表中重点看：

```
Region Hit@5
```

它不是公平 baseline，也不能部署，因为实际系统不知道正确页；它是上界实验。若该指标明显高于 `0.8313`，说明区域级节点检索还有提升空间；若接近 `0.8313`，说明当前 Evidence Set 已接近这套节点切分和 BGE 排序的可达上限。

Oracle 上界实验已完成：

```
Oracle-Page -> Region
Region Hit@5: 0.9750
MRR:          0.6851
nDCG@5:       0.6614
```

这说明正确证据页已知时，当前节点切分与 BGE 区域排序能命中 `97.5%` 的问题。Evidence Set 的 `Region Hit@5=0.8313` 距离上界还有 `0.1437`，约 `23` 个问题，这是后续优化空间。

至此，全量 160 问题上的 baseline 诊断矩阵已经完成。它可以作为“方法探索结果”，但还不能直接作为小论文最终主表，因为目前所有问题都参与了规则、参数和方法设计。

下一步必须做公司级数据划分，而不是继续增加实验：

```
20 家公司、160 个问题
-> train：12 家公司，96 个问题
-> dev：4 家公司，32 个问题
-> test：4 家公司，32 个问题
```

用途：

```
train：分析问题与开发方法
dev：确定槽位规则、权重、Top-K 等参数
test：参数冻结后只跑一次，用于论文最终结果
```

原始 `qa_annotations_v2.csv` 和 `qa_annotations_v2_reviewed.csv` 不需要修改或重新生成；仍以 reviewed 版为唯一实际输入。接下来我建议创建固定的公司划分文件，并让数据准备和检索命令能够按 `train/dev/test` 读取数据。这样从下一轮开始，实验才具备论文级别的可信性。